---
phase: 04-visual-polish
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [lib/features/chronological_list/presentation/item_list_screen.dart, lib/core/database/app_database.dart]
autonomous: true
requirements: [ORG-06]
must_haves:
  truths:
    - "Main screen displays a horizontal bar of labels (tags/folders)"
    - "Tapping a label in the bar filters the list immediately"
    - "The bar highlights the currently active filter"
  artifacts:
    - path: "lib/features/chronological_list/presentation/item_list_screen.dart"
      provides: "Horizontal QuickFilterBar UI"
---

<objective>
Implement a high-accessibility filtering bar on the main screen for quick discovery.

Purpose: Reduce the number of taps required to switch between common categories and tags.
Output: A horizontal scrolling bar of labels on the main screen.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/app_database.dart
@lib/features/chronological_list/presentation/item_list_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare Database for Quick Filtering</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
    Ensure `watchAllLabels` provides enough information. 
    - (Optional but recommended) Implement a method to get "frequent" or "recent" labels if we want to limit the bar's size. For now, all labels will be shown.
  </action>
  <verify>
    <automated>Existing watchAllLabels tests</automated>
  </verify>
  <done>Labels are available for the UI</done>
</task>

<task type="auto">
  <name>Task 2: Implement QuickFilterBar Widget</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Create a private `_QuickFilterBar` widget inside `item_list_screen.dart` or as a helper.
    - Horizontal `ListView` with `ChoiceChip` or custom `ActionChip` items.
    - Show an 'All' option followed by folders and tags.
  </action>
  <verify>
    <manual>Open the app and see a horizontal bar below the app bar</manual>
  </verify>
  <done>UI for quick filtering is present</done>
</task>

<task type="auto">
  <name>Task 3: Integrate QuickFilterBar into ItemListScreen</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Insert the `_QuickFilterBar` between the `AppBar` and the `ItemList`.
    - Ensure it updates the `_selectedLabel` state and triggers a list refresh.
    - Sync state between the Drawer and the QuickFilterBar.
  </action>
  <verify>
    <manual>Tap a tag in the bar and verify the list filters correctly and the tag highlights</manual>
  </verify>
  <done>Quick filtering is functional and synchronized</done>
</task>

</tasks>

<verification>
Verify that tapping labels in the horizontal bar correctly filters the items and provides visual feedback for the selection.
</verification>

<success_criteria>
- Horizontal bar is visible on the main screen.
- One-tap filtering works for all tags and folders.
- Selected filter is visually distinct in the bar.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-02-SUMMARY.md`
</output>
