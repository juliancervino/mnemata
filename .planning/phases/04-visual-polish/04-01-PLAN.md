---
phase: 04-visual-polish
plan: 01
type: execute
wave: 1
depends_on: [03-03]
files_modified: [lib/core/database/tables.dart, lib/core/database/app_database.dart, lib/features/chronological_list/presentation/item_list_screen.dart, lib/features/ingestion/services/extraction_service.dart]
autonomous: true
requirements: [ORG-05, CON-04]
must_haves:
  truths:
    - "List items display colored dots representing their assigned tags"
    - "URL items display a representative thumbnail or favicon"
  artifacts:
    - path: "lib/core/database/tables.dart"
      provides: "thumbnailUrl column"
    - path: "lib/features/ingestion/services/extraction_service.dart"
      provides: "Thumbnail extraction logic"
---

<objective>
Enhance the visual presentation of items in the main list with tag indicators and content previews.

Purpose: Make it easier to distinguish items at a glance and provide immediate context through thumbnails and tag colors.
Output: Thumbnails in list items and colored tag dots.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/tables.dart
@lib/features/chronological_list/presentation/item_list_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Thumbnail Support to Database</name>
  <files>lib/core/database/tables.dart, lib/core/database/app_database.dart</files>
  <action>
    Add `thumbnailUrl` (text, nullable) to `MnemataItems` table.
    - Update `AppDatabase` to version 4 and handle migration.
    - Update `updateItemContent` to also accept and save `thumbnailUrl`.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Database supports thumbnail URLs</done>
</task>

<task type="auto">
  <name>Task 2: Implement Thumbnail Extraction</name>
  <files>pubspec.yaml, lib/features/ingestion/services/extraction_service.dart</files>
  <action>
    Add `metadata_fetch` and `favicon` dependencies.
    - Update `ExtractionService` to extract the best available image (Open Graph or Favicon) from the URL.
    - Save the thumbnail URL to the database during extraction.
  </action>
  <verify>
    <manual>Share a URL and verify that a thumbnail URL is saved in the database</manual>
  </verify>
  <done>Representative images are extracted automatically</done>
</task>

<task type="auto">
  <name>Task 3: Display Thumbnails and Tag Dots in List</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Update `_ItemTile` to:
    - Display the extracted thumbnail/favicon in the leading position (replacing or overlaying the icon).
    - Use a `StreamBuilder` to watch tags for the item.
    - Display small colored dots (Wrap or Row) below the title/subtitle area to represent assigned tags.
  </action>
  <verify>
    <manual>Open the app and see thumbnails and color dots on items with tags</manual>
  </verify>
  <done>Main list is visually enhanced with previews and tag indicators</done>
</task>

</tasks>

<verification>
Verify that thumbnails load correctly and that tag dots reflect the actual colors assigned to the tags.
</verification>

<success_criteria>
- URL items show images or high-res favicons.
- Items with tags show small colored dots in the list view.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish/04-01-SUMMARY.md`
</output>
