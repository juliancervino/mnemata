# Phase 10-02: UI List Optimization & Ingestion Refinement

## Goal
Optimize the main list for information density and refine the ingestion screen layout for better productivity.

## Tasks
- [x] **UI-10-04**: Reorder `IngestionSummaryScreen` layout: Move Content Preview below the Tag list.
- [x] **UI-10-05**: Implement pre-selection of auto-tags (Year and Domain) in `IngestionSummaryScreen`.
- [x] **UI-10-06**: Optimize `_ItemTile` layout:
    - 80%+ width for title.
    - Show domain explicitly (e.g., `elpais.com`) instead of full URL.
    - Reduce footprint of drag handle and tag icons.
- [x] **UI-10-07**: Update `ItemEditorScreen` if necessary to match the new style.

## Implementation Strategy
1.  **Ingestion Refinement**:
    - Modify `_IngestionSummaryScreenState.initState` to identify and pre-select auto-tags (Year and Domain).
    - Swap the positions of the Content Preview block and the Label Assignment block in `build`.
2.  **List Tile Optimization**:
    - Update `_ItemTile` to use a `Row` with flexible widths.
    - Use `Uri.parse(item.url).host` to extract and display the domain.
    - Minimize padding and icon sizes for a denser look.

## Verification
- [ ] Ingestion screen shows tags before content preview.
- [ ] Year and Domain tags are automatically checked when entering the ingestion screen.
- [ ] Main list displays more items and clearly shows domains.
