# Phase 10-03: Advanced Sharing & UI Refinements

## Goal
Enhance content portability with rich sharing, improve system visibility in About, and polish ingestion/list layouts.

## Tasks
- [x] **UI-10-08**: Implement Rich Sharing for WhatsApp: Title + Subtitle + Link + Offline Text.
- [x] **UI-10-09**: Display build number (timestamp) in `AboutScreen`.
- [x] **UI-10-10**: Correct layout order in `IngestionSummaryScreen`: "Assign Labels" MUST be before "Content Preview".
- [x] **UI-10-11**: Show original domain for archive.today/archive.ph links in `_ItemTile`.
- [x] **UI-10-12**: Add narrow vertical scrollbar to the `ReaderScreen`.

## Implementation Strategy
1.  **Rich Sharing**:
    - Update `_ItemTile` and `ReaderScreen` share methods to fetch full content and format a text block.
2.  **About Build Info**:
    - Ensure `package_info_plus` is used to display `buildNumber`. 
3.  **Ingestion Layout**:
    - Re-verify and fix the `Column` children order in `IngestionSummaryScreen`.
4.  **Archive Domain**:
    - Apply the extraction logic (already used for tagging) to the subtitle/domain display in `_ItemTile`.
5.  **Scrollbar**:
    - Wrap the `SingleChildScrollView` or `ListView` in `ReaderScreen` with a `RawScrollbar` or `Scrollbar` with custom thickness.

## Verification
- [ ] Shared content in WhatsApp includes the full article text.
- [ ] About screen shows a timestamp-like build number.
- [ ] Ingestion screen layout matches the requested order.
- [ ] Archive links show the original source domain.
- [ ] Article viewer has a visible scrollbar.
