# Phase 10-01: Multi-select & UI Polish

## Goal
Introduce multi-selection mode for bulk operations and fix UI overlap issues.

## Tasks
- [x] **UI-10-01**: Fix Drawer overlap with system navigation bars using `SafeArea`.
- [x] **UI-10-02**: Implement multi-selection logic in `ItemListScreen`.
- [x] **UI-10-03**: Add bulk action bar (Delete, Assign Tags, Share).

## Implementation Strategy
1.  **Drawer Fix**: Wrap `Drawer` content in `SafeArea` to prevent overlap with system buttons. (Done)
2.  **Multi-select**:
    - Add `_isMultiSelectMode` and `_selectedItemIds` state. (Done)
    - Long-press on `_ItemTile` to activate mode. (Done)
    - Update `_ItemTile` to show selection state. (Done)
3.  **Bulk Actions**:
    - Show a `BottomAppBar` or overlay when items are selected. (Done)
    - Implement batch deletion and tagging in `AppDatabase`. (Done)

## Verification
- [x] Drawer does not overlap with system navigation bar (Android/iOS).
- [x] Long-pressing an item enters multi-select mode.
- [x] Selecting multiple items and performing a bulk delete works as expected.
