---
phase: 07-refined-control-personalization
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified: [lib/features/chronological_list/presentation/item_list_screen.dart, lib/core/database/app_database.dart]
autonomous: true
requirements: [ORG-09]
must_haves:
  truths:
    - "User can manually reorder items in the main list using a drag handle"
    - "The new order is persisted in the database via the sortOrder column"
    - "Reordering works correctly alongside Slidable swipe gestures"
  artifacts:
    - path: "lib/features/chronological_list/presentation/item_list_screen.dart"
      provides: "ReorderableListView integration"
---

<objective>
Implement manual drag-and-drop reordering for the main list items and persist the custom order.

Purpose: Give users total control over the priority and arrangement of their saved content.
Output: A reorderable list with persistent custom sorting.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/features/chronological_list/presentation/item_list_screen.dart
@lib/core/database/app_database.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sort Order Update Method to Database</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
    - Add `updateItemSortOrder(int id, int newOrder)` to `AppDatabase`.
    - (Optional) Add a batch update method if many items change at once, but individual updates are sufficient for now.
  </action>
  <verify>
    <manual>Manually call the method and check DB state.</manual>
  </verify>
  <done>Database supports updating order positions</done>
</task>

<task type="auto">
  <name>Task 2: Convert ListView to ReorderableListView</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    - Replace the `ListView.builder` in `ItemListScreen` with `ReorderableListView.builder`.
    - Set `buildDefaultDragHandles: false` to avoid conflicts with swiping.
    - Implement the `onReorder` callback:
      1. Calculate new `sortOrder` values for the affected items.
      2. Call `database.updateItemSortOrder` for each changed item.
  </action>
  <verify>
    <manual>Try to drag an item; it should show reorder feedback but not move yet without a handle.</manual>
  </verify>
  <done>List widget supports reordering logic</done>
</task>

<task type="auto">
  <name>Task 3: Add Custom Drag Handle to Item Tiles</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    - Add a `ReorderableDragStartListener` to the `_ItemTile`.
    - Use `Icons.drag_handle` as the visual indicator in the trailing section (alongside the label icon).
  </action>
  <verify>
    <manual>Drag an item using the handle and verify it stays in the new position after the list refreshes.</manual>
  </verify>
  <done>User can intuitively reorder items via a dedicated handle</done>
</task>

</tasks>

<verification>
Verify that dragging an item to a new position persists after app restart and that swipe gestures still work on the rest of the tile.
</verification>

<success_criteria>
- Main list supports drag-and-drop reordering via a handle.
- New order is saved to the database.
- Swiping (Share/Edit) still functions correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-refined-control-personalization/07-03-SUMMARY.md`
</output>
