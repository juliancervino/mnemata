---
phase: 07-refined-control-personalization
plan: 01
type: execute
wave: 1
depends_on: [03-01, 03-02]
files_modified: [pubspec.yaml, lib/core/database/tables.dart, lib/core/database/app_database.dart, lib/features/organization/presentation/label_manager_screen.dart]
autonomous: true
requirements: [ORG-07, ORG-08, ORG-09]
must_haves:
  truths:
    - "User can pick colors for labels via a color picker"
    - "User can rename existing labels"
    - "Database schema supports manual item ordering"
  artifacts:
    - path: "lib/features/organization/presentation/label_manager_screen.dart"
      provides: "Color selection and rename logic"
---

<objective>
Enable label customization (colors/renaming) and prepare the database for manual reordering.

Purpose: Allow users to personalize their organization system and lay the groundwork for drag-and-drop.
Output: Enhanced Label Manager and updated DB schema.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/tables.dart
@lib/features/organization/presentation/label_manager_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sort Order and Color Support to Database</name>
  <files>lib/core/database/tables.dart, lib/core/database/app_database.dart</files>
  <action>
    - Add `sort_order` (int, default 0) to `MnemataItems` table.
    - Update `AppDatabase` to version 5 and handle migration.
    - Update `watchAllItems` and related queries to use `sort_order` as the primary sort key (then `createdAt` desc).
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Database supports manual ordering</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Label Manager UI</name>
  <files>pubspec.yaml, lib/features/organization/presentation/label_manager_screen.dart</files>
  <action>
    - Add `flutter_colorpicker` dependency.
    - Add an edit icon to each label tile.
    - Implement an "Edit Label" dialog that allows renaming and picking a color.
    - Update the "Add Label" section to include a color preview and picker.
  </action>
  <verify>
    <manual>Open Label Manager, create a label with a specific color, and rename an existing one.</manual>
  </verify>
  <done>Labels are fully customizable</done>
</task>

</tasks>

<verification>
Ensure that color changes persist and that renaming a label updates correctly in the list and drawer.
</verification>

<success_criteria>
- Labels can be renamed.
- Label colors can be selected from a palette.
- Items have a sort order field ready for the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/07-refined-control-personalization/07-01-SUMMARY.md`
</output>
