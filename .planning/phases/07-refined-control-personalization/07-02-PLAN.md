---
phase: 07-refined-control-personalization
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [lib/features/chronological_list/presentation/item_list_screen.dart, lib/features/chronological_list/presentation/item_editor_screen.dart, lib/core/database/app_database.dart]
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "Swiping an item past a threshold triggers the action directly (no second tap)"
    - "Swiping left opens an edit screen for the item"
    - "User can edit title, URL, and labels in the edit screen"
  artifacts:
    - path: "lib/features/chronological_list/presentation/item_editor_screen.dart"
      provides: "Full-screen item editor UI"
---

<objective>
Implement high-efficiency swipe interactions and a dedicated item editor.

Purpose: Reduce friction for common actions and provide a way to correct or reorganize items after they've been saved.
Output: Direct-trigger slidable items and an Item Editor.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/features/chronological_list/presentation/item_list_screen.dart
@lib/core/database/app_database.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ItemEditorScreen</name>
  <files>lib/features/chronological_list/presentation/item_editor_screen.dart</files>
  <action>
    Create `lib/features/chronological_list/presentation/item_editor_screen.dart`.
    - Editable fields: Title, URL.
    - Label selection: Reuse the `FilterChip` logic from `IngestionSummaryScreen`.
    - Database: Methods to update title/URL and refresh label assignments.
  </action>
  <verify>
    <manual>Open the editor, change the title, save, and verify in the main list.</manual>
  </verify>
  <done>Items can be fully edited</done>
</task>

<task type="auto">
  <name>Task 2: Implement Direct Swipe Actions</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Update `Slidable` in `_ItemTile`:
    - Add `DismissiblePane` to both `startActionPane` and `endActionPane`.
    - Right Swipe (Start): Share action.
    - Left Swipe (End): Edit action (opens `ItemEditorScreen`).
    - Use `confirmDismiss: () async { ...; return false; }` to trigger the logic without removing the widget from the list.
  </action>
  <verify>
    <manual>Swipe an item all the way to the left/right and verify the action triggers instantly.</manual>
  </verify>
  <done>Swipe gestures are high-frictionless</done>
</task>

<task type="auto">
  <name>Task 3: Update Database for Item Editing</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
    Add `updateItemDetails(int id, String title, String? url)` to `AppDatabase`.
    Add `replaceAllLabelsForItem(int itemId, List<int> labelIds)` helper or update the editor to handle individual assignments.
  </action>
  <verify>
    <automated>Existing update tests or new ones for detail updates.</automated>
  </verify>
  <done>Database supports full item updates</done>
</task>

</tasks>

<verification>
Ensure that swiping triggers the correct action (Share/Edit) directly and that edits are persisted across app restarts.
</verification>

<success_criteria>
- One-swipe threshold triggers Share/Edit.
- Left-swipe opens the editor.
- Edits to title, URL, and labels are saved correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-refined-control-personalization/07-02-SUMMARY.md`
</output>
