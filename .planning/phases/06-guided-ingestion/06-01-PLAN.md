---
phase: 06-guided-ingestion
plan: 01
type: execute
wave: 1
depends_on: [03-02, 05-01]
files_modified: [lib/main.dart, lib/features/ingestion/services/share_service.dart, lib/features/ingestion/presentation/ingestion_summary_screen.dart]
autonomous: true
requirements: [ING-05]
must_haves:
  truths:
    - "Sharing a link/file opens a summary screen before saving"
    - "User can edit the title of the shared item"
    - "User can assign tags/folders to the item before it is saved to the database"
  artifacts:
    - path: "lib/features/ingestion/presentation/ingestion_summary_screen.dart"
      provides: "Pre-save editor UI"
---

<objective>
Provide a guided ingestion experience that allows users to review, edit, and organize content before it enters the repository.

Purpose: Prevent clutter and ensure items are correctly categorized from the moment they are saved.
Output: An interactive summary/editor screen triggered by share intents.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/features/ingestion/services/share_service.dart
@lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Global Navigator Key</name>
  <files>lib/main.dart</files>
  <action>
    Define a global `NavigatorState` key in `main.dart` and assign it to the `MaterialApp`.
    Expose it via `GetIt` or as a top-level variable so `ShareService` can use it for navigation.
  </action>
  <verify>
    <automated>Verify MaterialApp has the navigatorKey assigned</automated>
  </verify>
  <done>Services can trigger navigation</done>
</task>

<task type="auto">
  <name>Task 2: Implement IngestionSummaryScreen</name>
  <files>lib/features/ingestion/presentation/ingestion_summary_screen.dart</files>
  <action>
    Create the summary screen.
    - Fields: Editable Title, Read-only URL/Path, Thumbnail preview.
    - Integration: Reuse `LabelSelectorSheet` logic or a simplified version to pick tags/folders.
    - Actions: "Save" (inserts into DB) and "Discard" (deletes temp files/ignores).
  </action>
  <verify>
    <manual>Manually navigate to this screen with dummy data and verify Save works</manual>
  </verify>
  <done>User can edit and organize items before saving</done>
</task>

<task type="auto">
  <name>Task 3: Refactor ShareService for Guided Ingestion</name>
  <files>lib/features/ingestion/services/share_service.dart</files>
  <action>
    Modify `handleUrl` and `handleFile` to:
    1. Perform extraction (metadata/thumbnails/text) immediately.
    2. Instead of calling `_database.insertItem`, navigate to `IngestionSummaryScreen` with the extracted data.
    3. Pass necessary data (title, content, url, filePath, thumbnailUrl) to the screen.
  </action>
  <verify>
    <manual>Share a URL from a browser and verify the app opens to the IngestionSummaryScreen</manual>
  </verify>
  <done>The ingestion flow is now interactive and guided</done>
</task>

</tasks>

<verification>
Ensure that items are ONLY saved to the database after the user clicks 'Save' on the summary screen, and that all edits (title, tags) are correctly persisted.
</verification>

<success_criteria>
- The app automatically navigates to the summary screen when content is shared.
- Users can change the title and select tags on the summary screen.
- Clicking 'Save' persists the item with all user modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/06-guided-ingestion/06-01-SUMMARY.md`
</output>
