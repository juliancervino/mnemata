# Plan: Phase 08 Plan 01 - Structural Simplification & Ingestion Robustness

## Objective
Remove the "Folder" concept to simplify organization, ensure shared content is captured even during app cold starts, and improve URL extraction from shared text.

## Tasks
1. **Remove Folders (Task 5)**
    - [ ] Update `lib/features/chronological_list/presentation/item_list_screen.dart` to remove folder filtering logic and update the AppBar icon (folder -> tag).
    - [ ] Update `lib/features/organization/presentation/label_manager_screen.dart` to remove the "Folder?" switch and related icons.
    - [ ] Update `lib/features/organization/presentation/label_selector_sheet.dart` and other UI components to use tag icons exclusively.
    - [ ] (Optional) Remove `isFolder` from `tables.drift` and migrate, or simply ignore it in the UI and set it to false by default.

2. **Fix Cold Start Sharing (Task 2)**
    - [ ] Move `ShareService.init()` from `ItemListScreen.initState` to a more central location (like `main.dart` or after `setupLocator`) to ensure it runs immediately.
    - [ ] Verify if `getInitialMedia` correctly triggers the summary screen on cold start.

3. **URL Extraction from Text (Task 9)**
    - [ ] Update `ShareService.handleUrl` in `lib/features/ingestion/services/share_service.dart`.
    - [ ] Implement Regex to find the first URL in the shared string if it's not a pure URL.

4. **Manual URL Addition (Task 8)**
    - [ ] Add a Floating Action Button (FAB) to `ItemListScreen`.
    - [ ] Create a simple dialog/sheet to input a URL manually.
    - [ ] Route the input URL to `ShareService.handleUrl`.

## Verification Plan
### Automated Tests
- [ ] Test `ShareService.handleUrl` with strings containing text + URL.
- [ ] Test `AppDatabase` queries for labels (ensure they still work without folder distinction).

### Manual Verification
- [ ] Share a link while the app is completely closed and verify the summary screen appears.
- [ ] Verify that no folder icons or "Folder?" switches are visible in the app.
- [ ] Use the new FAB to add a URL manually and verify it's saved.
