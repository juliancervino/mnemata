# Plan: Phase 08 Plan 03 - Visual Polish & Documentation

## Objective
Finalize UI/UX improvements including tag colors, `SafeArea` fixes, showing tags in the reader view, and creating a comprehensive `README.md`.

## Tasks
1. **Tag Color Consistency (Task 4)**
    - [x] Update `lib/features/chronological_list/presentation/item_list_screen.dart` drawer to correctly display tag colors.
    - [x] Update `lib/features/organization/presentation/label_selector_sheet.dart` to respect label colors in icons/chips.

2. **SafeArea/Padding Fixes (Task 10)**
    - [x] Wrap the `Scaffold.body` in `lib/features/reader/presentation/reader_screen.dart` with `SafeArea`.
    - [x] Add `SafeArea` to `lib/features/ingestion/presentation/ingestion_summary_screen.dart` or add extra bottom padding to the `SingleChildScrollView`.

3. **Tags in Reader View (Task 11)**
    - [x] Update `ReaderScreen` to fetch and display labels associated with the current item.
    - [x] Add a `Wrap` of `Chip` or `FilterChip` (read-only) below the title.

4. **Comprehensive README (Task 3)**
    - [x] Create `README.md` at project root.
    - [x] Include description, core values, platform support, and technical architecture (Flutter, Drift, FTS5).
    - [x] Detail the extraction system (Readability) and ingestion flow.

## Verification Plan
### Automated Tests
- [x] No new tests required for purely UI changes.

### Manual Verification
- [x] Verify that the reader screen correctly displays tags with their specific colors.
- [x] Verify that on Android with button navigation enabled, the content in `ReaderScreen` and `IngestionSummaryScreen` is not obscured by system buttons.
- [x] Check the `README.md` for clarity and completeness.
