# Plan: Phase 08 Plan 02 - Advanced Organization & Interaction

## Objective
Implement multi-tag "AND" filtering, enable tag creation during ingestion, and fix the `Slidable` auto-close behavior.

## Tasks
1. **Multi-tag "AND" Filtering (Task 6)**
    - [ ] Update `ItemListScreen` to store a `Set<int>` of selected label IDs.
    - [ ] Add `watchItemsByMultipleLabels(List<int> labelIds)` to `AppDatabase` in `lib/core/database/app_database.dart`.
    - [ ] Implement the logic in `ItemListScreen` to toggle labels in the set and re-trigger the stream.

2. **Create Tags During Ingestion/Editing (Task 7)**
    - [ ] Add an "Add Tag" button or `FilterChip` in `IngestionSummaryScreen` and `ItemEditorScreen` that opens a simple name/color dialog.
    - [ ] Save the new tag to the database and update the local state.

3. **Slidable Auto-close (Task 1)**
    - [ ] Add `Slidable.of(context)?.close()` or use a `controller` in `Slidable` actions in `lib/features/chronological_list/presentation/item_list_screen.dart`.
    - [ ] Verify that swiping and tapping an action (Edit/Share) closes the item upon returning.

## Verification Plan
### Automated Tests
- [ ] Test `watchItemsByMultipleLabels` with 1, 2, and 0 labels.
- [ ] Test `insertLabel` from various screens.

### Manual Verification
- [ ] Select multiple tags in the Drawer or QuickFilterBar and verify the list shows only items with *all* selected tags.
- [ ] Create a new tag directly from the `IngestionSummaryScreen`.
- [ ] Perform a swipe action (Edit/Share) and verify the list item is no longer "slid" open when finished.
