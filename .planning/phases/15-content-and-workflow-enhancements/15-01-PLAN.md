---
phase: 15-content-and-workflow-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/ingestion/services/author_extraction_service.dart
  - lib/features/ingestion/services/extraction_service.dart
  - lib/features/ingestion/services/share_service.dart
  - lib/core/database/tables.dart
  - lib/core/database/app_database.dart
  - lib/features/chronological_list/presentation/item_list_screen.dart
  - lib/features/reader/presentation/reader_screen.dart
  - test/features/ingestion/services/author_extraction_service_test.dart
  - test/core/database/app_database_author_test.dart
autonomous: true
requirements:
  - AUT-01
  - AUT-02
must_haves:
  truths:
    - "Author metadata is extracted through a dedicated author module and title/body extraction behavior is unchanged."
    - "Author metadata persists on items and is available to item list and reader flows."
  artifacts:
    - path: lib/features/ingestion/services/author_extraction_service.dart
      provides: Isolated author extraction logic with no title/body ownership.
    - path: lib/core/database/tables.dart
      provides: Nullable author column for persisted metadata.
    - path: lib/core/database/app_database.dart
      provides: Migration + write/read support for author metadata.
    - path: test/features/ingestion/services/author_extraction_service_test.dart
      provides: Regression safety for dedicated author extraction behavior.
  key_links:
    - from: lib/features/ingestion/services/share_service.dart
      to: lib/features/ingestion/services/author_extraction_service.dart
      via: ingestion enrichment call
      pattern: extractAuthor
    - from: lib/features/ingestion/services/share_service.dart
      to: lib/core/database/app_database.dart
      via: insert/update with author field
      pattern: MnemataItemsCompanion
---

<objective>
Deliver AUT-01 and AUT-02 by introducing a dedicated author extraction module and persisting author metadata through Drift without changing existing title/body extraction internals.

Purpose: Add requested author capability while preserving ingestion stability.
Output: New author service, DB schema/migration updates, item-view availability, and focused automated tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-and-workflow-enhancements/15-CONTEXT.md
@.planning/phases/15-content-and-workflow-enhancements/15-RESEARCH.md
@lib/features/ingestion/services/extraction_service.dart
@lib/features/ingestion/services/share_service.dart
@lib/core/database/tables.dart
@lib/core/database/app_database.dart
@lib/features/chronological_list/presentation/item_list_screen.dart
@lib/features/reader/presentation/reader_screen.dart

<interfaces>
From lib/features/ingestion/services/extraction_service.dart:
```dart
class ExtractionService {
  Future<({String title, String content, String? thumbnailUrl})?> extractContent(String url)
}
```

From lib/core/database/tables.dart:
```dart
class MnemataItems extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get title => text().nullable()();
  TextColumn get content => text().nullable()();
  TextColumn get type => text()();
}
```

From lib/core/database/app_database.dart:
```dart
Future<int> insertItem(MnemataItemsCompanion item);
Future<void> updateItemContent(int id, String content, String? title, String? thumbnailUrl);
int get schemaVersion => 7;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add author extraction and persistence test scaffolds</name>
  <files>test/features/ingestion/services/author_extraction_service_test.dart, test/core/database/app_database_author_test.dart</files>
  <behavior>
    - Test 1: author service extracts prioritized author candidates from metadata/html and returns null when absent.
    - Test 2: title/body extraction contract from ExtractionService remains unchanged after author module integration.
    - Test 3: author column persists through insert/read and migration for existing rows.
  </behavior>
  <action>Create failing tests first for AUT-01 and AUT-02, explicitly asserting D-01 and D-02: dedicated author module and no modification of title/body behavior.</action>
  <verify>
    <automated>flutter test test/features/ingestion/services/author_extraction_service_test.dart test/core/database/app_database_author_test.dart</automated>
  </verify>
  <done>Tests fail for missing author module/field and become executable anchors for implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dedicated author extraction module and schema migration</name>
  <files>lib/features/ingestion/services/author_extraction_service.dart, lib/features/ingestion/services/share_service.dart, lib/core/database/tables.dart, lib/core/database/app_database.dart</files>
  <behavior>
    - Test 1: ingestion path stores author in item record when author data is available.
    - Test 2: extraction_service title/content outputs remain byte-for-byte equivalent for existing fixtures.
  </behavior>
  <action>Implement author extraction in new service only (per D-01), wire ingestion to call it as additive enrichment, add nullable author column and migration, and keep ExtractionService title/body logic unchanged (per D-02).</action>
  <verify>
    <automated>flutter test test/features/ingestion/services/author_extraction_service_test.dart test/features/ingestion/extraction_service_test.dart test/core/database/app_database_author_test.dart && flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Author metadata is extracted and persisted while existing title/body extraction tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose persisted author in item views</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart, lib/features/reader/presentation/reader_screen.dart, test/features/chronological_list/presentation/item_list_screen_test.dart</files>
  <behavior>
    - Test 1: item list renders author metadata when present and preserves existing fallback subtitle behavior when absent.
    - Test 2: reader share/read surfaces can access item author without null crashes.
  </behavior>
  <action>Surface persisted author in item views for AUT-02 using non-breaking UI additions and preserve existing interactions.</action>
  <verify>
    <automated>flutter test test/features/chronological_list/presentation/item_list_screen_test.dart</automated>
  </verify>
  <done>Author is visible/available in item views and all AUT-related tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web content -> author extraction module | Untrusted metadata/html author candidates cross into trusted parsing logic |
| extraction pipeline -> database write | Parsed author string crosses into persisted storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T | author_extraction_service.dart | mitigate | sanitize and trim author candidates, cap stored length, reject script-like payloads before persistence |
| T-15-02 | D | share_service.dart ingestion path | mitigate | keep author extraction non-blocking and fallback-safe so ingestion still succeeds if author parse fails |
| T-15-03 | I | item list and reader author rendering | accept | author is public metadata only; no new secret material introduced |
</threat_model>

<verification>
- Run targeted AUT tests after each task, then run flutter test on touched suites.
- Confirm existing extraction tests stay green to prove D-02 is preserved.
</verification>

<success_criteria>
- AUT-01: Dedicated author module exists and title/body module behavior is unchanged.
- AUT-02: Author metadata is persisted and visible/usable in item views.
</success_criteria>

<output>
After completion, create .planning/phases/15-content-and-workflow-enhancements/15-01-SUMMARY.md
</output>
