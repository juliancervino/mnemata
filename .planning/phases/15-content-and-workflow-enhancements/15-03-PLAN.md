---
phase: 15-content-and-workflow-enhancements
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - pubspec.yaml
  - lib/features/bookmarks/services/bookmark_export_service.dart
  - lib/features/bookmarks/services/bookmark_import_service.dart
  - lib/features/settings/presentation/settings_screen.dart
  - lib/core/database/app_database.dart
  - test/features/bookmarks/bookmark_export_service_test.dart
  - test/features/bookmarks/bookmark_import_service_test.dart
  - test/features/settings/settings_backup_actions_test.dart
autonomous: true
requirements:
  - BKM-01
  - BKM-02
must_haves:
  truths:
    - "User can export saved URLs in Netscape Bookmark HTML format."
    - "User can import URLs from Netscape Bookmark HTML while ignoring unsupported entries."
  artifacts:
    - path: lib/features/bookmarks/services/bookmark_export_service.dart
      provides: URLs-only Netscape HTML writer.
    - path: lib/features/bookmarks/services/bookmark_import_service.dart
      provides: Netscape HTML parser + validated URL extraction.
    - path: lib/features/settings/presentation/settings_screen.dart
      provides: import/export user actions.
  key_links:
    - from: lib/features/settings/presentation/settings_screen.dart
      to: lib/features/bookmarks/services/bookmark_export_service.dart
      via: export action
      pattern: exportBookmarks
    - from: lib/features/settings/presentation/settings_screen.dart
      to: lib/features/bookmarks/services/bookmark_import_service.dart
      via: import action
      pattern: importBookmarks
---

<objective>
Implement URL bookmark interoperability using Netscape Bookmark HTML export and import flows for BKM-01 and BKM-02.

Purpose: Enable user portability into and out of Mnemata with a browser-compatible format.
Output: Bookmark import/export services, settings UI actions, and parser/exporter tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-and-workflow-enhancements/15-CONTEXT.md
@.planning/phases/15-content-and-workflow-enhancements/15-RESEARCH.md
@lib/features/settings/presentation/settings_screen.dart
@lib/core/database/app_database.dart
@pubspec.yaml

<interfaces>
From lib/core/database/app_database.dart:
```dart
Stream<List<MnemataItem>> watchAllItems();
Future<MnemataItem?> getItemByCanonicalUrl(String rawUrl);
Future<int> insertItem(MnemataItemsCompanion item);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing import/export format tests</name>
  <files>test/features/bookmarks/bookmark_export_service_test.dart, test/features/bookmarks/bookmark_import_service_test.dart</files>
  <behavior>
    - Test 1: exporter emits valid Netscape header and URL bookmark entries only (BKM-01, D-05).
    - Test 2: importer parses nested bookmark folders and returns only http/https URLs (BKM-02, D-05).
    - Test 3: malformed entries and non-URL nodes are skipped safely.
  </behavior>
  <action>Create red tests defining universal HTML compatibility and URLs-only scope per D-05 before implementing services.</action>
  <verify>
    <automated>flutter test test/features/bookmarks/bookmark_export_service_test.dart test/features/bookmarks/bookmark_import_service_test.dart</automated>
  </verify>
  <done>Executable tests define importer/exporter behavior and fail until implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Netscape HTML export and import services</name>
  <files>lib/features/bookmarks/services/bookmark_export_service.dart, lib/features/bookmarks/services/bookmark_import_service.dart, lib/core/database/app_database.dart</files>
  <behavior>
    - Test 1: export writes deterministic bookmarks HTML for current URL items only.
    - Test 2: import returns validated URL list and inserts non-duplicate records.
  </behavior>
  <action>Implement URL-only exporter/importer in dedicated services using html parser, enforce scheme allowlist and dedupe against canonical URL records, and keep non-URL content out of scope per D-05.</action>
  <verify>
    <automated>flutter test test/features/bookmarks/bookmark_export_service_test.dart test/features/bookmarks/bookmark_import_service_test.dart</automated>
  </verify>
  <done>Round-trip import/export for URLs works with universal bookmarks HTML compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire import/export actions into Settings workflow</name>
  <files>pubspec.yaml, lib/features/settings/presentation/settings_screen.dart</files>
  <behavior>
    - Test 1: settings export action generates bookmark html artifact and exposes share/save flow.
    - Test 2: settings import action selects html file and ingests URLs with success/error feedback.
  </behavior>
  <action>Add settings actions for bookmark export/import and include any required file-picker dependency wiring; extend existing settings actions test coverage in `settings_backup_actions_test.dart`; ensure imported scope remains URL-only per D-05.</action>
  <verify>
    <automated>flutter test test/features/bookmarks/bookmark_export_service_test.dart test/features/bookmarks/bookmark_import_service_test.dart test/features/settings/settings_backup_actions_test.dart</automated>
  </verify>
  <done>Users can trigger bookmark export/import end-to-end from Settings with clear feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external html bookmark file -> import parser | Untrusted file content crosses into parsing and DB insertion paths |
| internal URL set -> exported html file | App data crosses into shared/exported artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | T | bookmark_import_service.dart | mitigate | parse with DOM parser, accept only http/https href, skip script/javascript/data schemes |
| T-15-08 | D | bookmark_import_service.dart | mitigate | impose import file size cap and bounded parse failure handling with user feedback |
| T-15-09 | I | bookmark export flow | mitigate | export URLs only by design, exclude notes/content/body fields from generated html |
</threat_model>

<verification>
- Run bookmark import/export unit tests and settings workflow tests.
- Manual smoke: export file imports successfully into Chrome/Firefox bookmarks manager.
</verification>

<success_criteria>
- BKM-01: URL export generates Netscape Bookmark HTML.
- BKM-02: URL import parses same format and persists valid URLs.
</success_criteria>

<output>
After completion, create .planning/phases/15-content-and-workflow-enhancements/15-03-SUMMARY.md
</output>
