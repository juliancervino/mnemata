---
phase: 15-content-and-workflow-enhancements
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - lib/core/database/tables.dart
  - lib/core/database/app_database.dart
  - lib/features/settings/services/settings_service.dart
  - lib/features/settings/presentation/settings_screen.dart
  - lib/features/chronological_list/presentation/item_list_screen.dart
  - lib/features/chronological_list/presentation/recycle_bin_screen.dart
  - lib/features/chronological_list/services/recycle_purge_service.dart
  - lib/main.dart
  - test/core/database/app_database_recycle_bin_test.dart
  - test/features/settings/recycle_retention_settings_test.dart
  - test/features/chronological_list/services/recycle_purge_service_test.dart
autonomous: true
requirements:
  - TRS-01
  - TRS-02
  - TRS-03
must_haves:
  truths:
    - "Deleting items moves them into recycle state instead of immediate hard delete."
    - "Retention can be configured from Settings between 1 and 30 days."
    - "Expired recycle-bin items are permanently purged automatically."
  artifacts:
    - path: lib/core/database/tables.dart
      provides: deletedAt column for soft-delete state.
    - path: lib/features/settings/services/settings_service.dart
      provides: retention preference with clamp 1..30.
    - path: lib/features/chronological_list/services/recycle_purge_service.dart
      provides: cutoff-based permanent deletion logic.
    - path: lib/features/chronological_list/presentation/recycle_bin_screen.dart
      provides: recycle-bin browse/restore/purge UI.
  key_links:
    - from: lib/features/chronological_list/presentation/item_list_screen.dart
      to: lib/core/database/app_database.dart
      via: delete actions calling soft-delete APIs
      pattern: moveToRecycle
    - from: lib/main.dart
      to: lib/features/chronological_list/services/recycle_purge_service.dart
      via: startup purge trigger
      pattern: purgeExpired
---

<objective>
Implement recycle-bin lifecycle with configurable retention and automatic purge, covering TRS-01, TRS-02, and TRS-03.

Purpose: Prevent accidental permanent loss while keeping storage bounded by user policy.
Output: Soft-delete persistence, retention settings, recycle-bin UI, and automated purge execution.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-and-workflow-enhancements/15-CONTEXT.md
@lib/core/database/tables.dart
@lib/core/database/app_database.dart
@lib/features/settings/services/settings_service.dart
@lib/features/settings/presentation/settings_screen.dart
@lib/features/chronological_list/presentation/item_list_screen.dart
@lib/main.dart

<interfaces>
From lib/core/database/app_database.dart:
```dart
Future<int> deleteItem(int id);
Future<int> deleteItems(List<int> ids);
Stream<List<MnemataItem>> watchAllItems();
```

From lib/features/settings/services/settings_service.dart:
```dart
class SettingsService {
  int get backupMaxCount;
  Future<void> setBackupMaxCount(int value);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recycle lifecycle tests and retention settings tests</name>
  <files>test/core/database/app_database_recycle_bin_test.dart, test/features/settings/recycle_retention_settings_test.dart, test/features/chronological_list/services/recycle_purge_service_test.dart</files>
  <behavior>
    - Test 1: delete actions set deletedAt and remove item from active streams (TRS-01).
    - Test 2: retention setting clamps to 1..30 and persists correctly (TRS-02).
    - Test 3: purge service permanently deletes rows older than cutoff (TRS-03).
  </behavior>
  <action>Create failing tests first for D-03 and D-04 behavior and soft-delete query partitioning guarantees.</action>
  <verify>
    <automated>flutter test test/core/database/app_database_recycle_bin_test.dart test/features/settings/recycle_retention_settings_test.dart test/features/chronological_list/services/recycle_purge_service_test.dart</automated>
  </verify>
  <done>Test harnesses exist and enforce recycle, retention range, and purge behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement soft-delete persistence and recycle-bin UI flow</name>
  <files>lib/core/database/tables.dart, lib/core/database/app_database.dart, lib/features/chronological_list/presentation/item_list_screen.dart, lib/features/chronological_list/presentation/recycle_bin_screen.dart</files>
  <behavior>
    - Test 1: active item queries exclude deletedAt rows by default.
    - Test 2: recycle-bin screen lists deleted items and supports restore.
  </behavior>
  <action>Add deletedAt column + migration, replace hard-delete entry points with move-to-recycle semantics (per D-03), add dedicated recycle-bin UI for restore/permanent delete actions, and ensure search/list queries stay consistent.</action>
  <verify>
    <automated>flutter test test/core/database/app_database_recycle_bin_test.dart test/features/chronological_list/presentation/item_list_screen_test.dart && flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Primary delete flows are soft-delete and recycle-bin operations are functional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement retention policy setting and automatic purge trigger</name>
  <files>lib/features/settings/services/settings_service.dart, lib/features/settings/presentation/settings_screen.dart, lib/features/chronological_list/services/recycle_purge_service.dart, lib/main.dart</files>
  <behavior>
    - Test 1: settings UI only allows effective retention values 1..30 days.
    - Test 2: app startup triggers purge of expired recycle-bin items using configured retention window.
  </behavior>
  <action>Add recycle retention setting with clamp 1..30 (per D-03), implement cutoff-based auto purge (per D-04), and invoke purge during bootstrap without blocking app startup.</action>
  <verify>
    <automated>flutter test test/features/settings/recycle_retention_settings_test.dart test/features/chronological_list/services/recycle_purge_service_test.dart</automated>
  </verify>
  <done>Retention is user-configurable in Settings and expired items are purged automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI delete intent -> database mutation | User action crosses into destructive data lifecycle operation |
| settings value -> purge policy | Configured retention value controls permanent delete cutoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | T | app_database.dart soft-delete methods | mitigate | transactional updates, explicit active/recycle query scopes, and restore guards |
| T-15-05 | D | recycle_purge_service.dart | mitigate | clamp retention input to 1..30, purge in bounded batches, skip startup crash on purge failure |
| T-15-06 | R | recycle_bin_screen.dart actions | mitigate | log/trace delete-restore actions and show user-visible confirmation snackbars |
</threat_model>

<verification>
- Execute focused recycle/retention/purge tests, then run flutter test for regression confidence.
- Manually validate: delete item -> appears in recycle bin -> restore -> visible in active list.
</verification>

<success_criteria>
- TRS-01: Delete actions perform soft-delete to recycle bin.
- TRS-02: Retention setting is configurable and clamped from 1 to 30 days.
- TRS-03: Expired recycle-bin items are purged automatically.
</success_criteria>

<output>
After completion, create .planning/phases/15-content-and-workflow-enhancements/15-02-SUMMARY.md
</output>
