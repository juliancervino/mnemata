---
phase: 11-cloud-data-portability
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - lib/features/backup/services/backup_restore_service.dart
  - lib/features/backup/presentation/restore_preview_sheet.dart
  - lib/features/settings/presentation/settings_screen.dart
  - test/features/backup/backup_restore_service_test.dart
autonomous: true
requirements:
  - POR-01
must_haves:
  truths:
    - User can preview backup contents before restore execution.
    - Restore operation validates archive integrity before applying data.
    - Restore requires explicit user confirmation.
  artifacts:
    - path: lib/features/backup/services/backup_restore_service.dart
      provides: Preview and apply restore orchestration.
    - path: lib/features/backup/presentation/restore_preview_sheet.dart
      provides: Restore preview and explicit confirmation UI.
    - path: lib/features/settings/presentation/settings_screen.dart
      provides: Entry points for manual backup and restore preview flow.
  key_links:
    - from: lib/features/settings/presentation/settings_screen.dart
      to: lib/features/backup/services/backup_restore_service.dart
      via: User-triggered restore flow from settings.
      pattern: BackupRestoreService
    - from: lib/features/backup/services/backup_restore_service.dart
      to: lib/features/backup/services/backup_archive_service.dart
      via: Archive inspection and integrity validation.
      pattern: inspectArchiveRequiredEntries
---

<objective>
Implement restore preview and apply flows with integrity validation and explicit confirmation.

Purpose: Deliver safe, user-controlled recovery path for full backups.
Output: Restore service, restore preview UI, settings integration, and tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cloud-data-portability/11-CONTEXT.md
@.planning/phases/11-cloud-data-portability/11-RESEARCH.md
@lib/features/settings/presentation/settings_screen.dart
@lib/core/database/app_database.dart
@lib/main.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build restore preview and validation service layer</name>
  <files>lib/features/backup/services/backup_restore_service.dart, test/features/backup/backup_restore_service_test.dart</files>
  <read_first>lib/features/backup/services/backup_archive_service.dart, lib/core/database/app_database.dart</read_first>
  <behavior>
    - Test 1: previewBackup returns manifest metadata and required-entry status without mutating live data.
    - Test 2: applyRestore aborts and reports error when checksum validation fails.
  </behavior>
  <action>Implement BackupRestoreService with previewBackup and applyRestore APIs. previewBackup must parse manifest and required entry status only. applyRestore must re-validate checksums, use staging directory, and execute apply in all-or-nothing flow, aborting on validation mismatch. Add tests for preview non-mutation and checksum failure paths.</action>
  <acceptance_criteria>backup_restore_service.dart contains previewBackup and applyRestore; backup_restore_service_test.dart contains tests for non-mutating preview and checksum-failure abort; flutter test test/features/backup/backup_restore_service_test.dart exits 0.</acceptance_criteria>
  <verify>
    <automated>flutter test test/features/backup/backup_restore_service_test.dart</automated>
  </verify>
  <done>Restore service guarantees preview-before-apply and integrity-gated execution.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings UI restore preview and explicit confirmation gate</name>
  <files>lib/features/backup/presentation/restore_preview_sheet.dart, lib/features/settings/presentation/settings_screen.dart</files>
  <read_first>lib/features/settings/presentation/settings_screen.dart, lib/features/reader/presentation/reader_screen.dart</read_first>
  <action>Add restore entry in SettingsScreen that opens restore preview sheet. Create RestorePreviewSheet showing backup timestamp, item/file counts, and validation status. Provide explicit confirmation action separate from preview action. Disable apply action when validation status is failed.</action>
  <acceptance_criteria>settings_screen.dart contains user-triggered backup restore entry point; restore_preview_sheet.dart contains explicit confirmation control and disabled apply state on validation failure; flutter analyze exits 0.</acceptance_criteria>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>User can inspect backup details and must confirm explicitly before restore apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-selected backup file to restore service | Backup source is untrusted until validated. |
| restore preview to restore apply | UI state transition must not bypass confirmation gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | T | restore input archive | mitigate | Re-validate checksums at apply time, not only preview time. |
| T-11-05 | E | restore confirmation flow | mitigate | Require explicit confirmation action and disable apply when validation fails. |
| T-11-06 | D | restore execution | mitigate | Use staged apply and abort on first integrity or IO failure to avoid partial state. |
</threat_model>

<verification>
- flutter test test/features/backup/backup_restore_service_test.dart
- flutter analyze
</verification>

<success_criteria>
- Restore preview exists and is non-mutating.
- Integrity failures block restore apply.
- Explicit user confirmation is enforced in UI.
</success_criteria>

<output>
After completion, create .planning/phases/11-cloud-data-portability/11-02-SUMMARY.md
</output>
