---
phase: 11-cloud-data-portability
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - lib/features/backup/services/cloud_backup_provider.dart
  - lib/features/backup/services/google_drive_backup_provider.dart
  - lib/features/backup/services/backup_scheduler_service.dart
  - lib/features/settings/services/settings_service.dart
  - lib/main.dart
  - test/features/backup/backup_scheduler_service_test.dart
  - test/features/backup/google_drive_backup_provider_test.dart
autonomous: true
requirements:
  - POR-01
  - POR-02
must_haves:
  truths:
    - User can trigger backup to Google Drive through provider abstraction.
    - Automatic backup policy runs daily and respects Wi-Fi plus charging constraints.
    - Last backup metadata is persisted for user visibility and diagnostics.
  artifacts:
    - path: lib/features/backup/services/cloud_backup_provider.dart
      provides: Provider contract for cloud backup operations.
    - path: lib/features/backup/services/google_drive_backup_provider.dart
      provides: Google Drive provider implementation.
    - path: lib/features/backup/services/backup_scheduler_service.dart
      provides: Auto-backup policy orchestration.
    - path: lib/main.dart
      provides: Scheduler bootstrap wiring at app startup.
  key_links:
    - from: lib/main.dart
      to: lib/features/backup/services/backup_scheduler_service.dart
      via: Service initialization after DI setup.
      pattern: BackupSchedulerService
    - from: lib/features/backup/services/backup_scheduler_service.dart
      to: lib/features/settings/services/settings_service.dart
      via: Policy and last-run state read/write.
      pattern: SettingsService
---

<objective>
Deliver cloud upload boundary and scheduled backup execution policy for production-ready data portability.

Purpose: Complete POR-01 and POR-02 with operationally safe scheduling and Google Drive integration contract.
Output: Provider contracts, scheduler service, startup wiring, and automated coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cloud-data-portability/11-CONTEXT.md
@.planning/phases/11-cloud-data-portability/11-RESEARCH.md
@lib/main.dart
@lib/features/settings/services/settings_service.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define cloud provider contract and Google Drive implementation seam</name>
  <files>lib/features/backup/services/cloud_backup_provider.dart, lib/features/backup/services/google_drive_backup_provider.dart, test/features/backup/google_drive_backup_provider_test.dart</files>
  <read_first>lib/features/ingestion/services/share_service.dart, lib/main.dart</read_first>
  <behavior>
    - Test 1: Cloud provider contract supports upload, list backups, and download by backup ID.
    - Test 2: Google Drive provider maps provider errors into deterministic domain-level failures.
  </behavior>
  <action>Create CloudBackupProvider interface with uploadBackup, listBackups, and downloadBackup methods. Implement GoogleDriveBackupProvider skeleton with concrete request/response mapping and deterministic error mapping classes used by scheduler and manual backup flows. Add tests for contract conformance and error mapping behavior.</action>
  <acceptance_criteria>cloud_backup_provider.dart defines abstract class CloudBackupProvider with upload/list/download methods; google_drive_backup_provider.dart defines GoogleDriveBackupProvider implementing contract; google_drive_backup_provider_test.dart verifies deterministic error mapping; flutter test test/features/backup/google_drive_backup_provider_test.dart exits 0.</acceptance_criteria>
  <verify>
    <automated>flutter test test/features/backup/google_drive_backup_provider_test.dart</automated>
  </verify>
  <done>Cloud provider seam is stable and Google Drive implementation path is consumable by scheduler/manual flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scheduler policy engine and settings persistence for backup metadata</name>
  <files>lib/features/backup/services/backup_scheduler_service.dart, lib/features/settings/services/settings_service.dart, test/features/backup/backup_scheduler_service_test.dart</files>
  <read_first>lib/features/settings/services/settings_service.dart, lib/main.dart</read_first>
  <behavior>
    - Test 1: Scheduler executes daily when policy constraints are met.
    - Test 2: Scheduler skips execution and records reason when Wi-Fi or charging constraints are unmet.
  </behavior>
  <action>Add BackupSchedulerService with runIfDue and evaluatePolicy methods implementing daily cadence plus Wi-Fi and charging checks. Extend SettingsService with backup schedule preference keys, lastSuccessfulBackupAt, and lastFailureReason state. Add tests for due-run path and policy-skip path with explicit reason codes.</action>
  <acceptance_criteria>backup_scheduler_service.dart contains runIfDue and evaluatePolicy; settings_service.dart contains backup schedule and last-run metadata getters/setters; backup_scheduler_service_test.dart contains due-run and policy-skip tests; flutter test test/features/backup/backup_scheduler_service_test.dart exits 0.</acceptance_criteria>
  <verify>
    <automated>flutter test test/features/backup/backup_scheduler_service_test.dart</automated>
  </verify>
  <done>Scheduler policy behavior is deterministic and persisted state supports user diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Wire scheduler bootstrap at app startup</name>
  <files>lib/main.dart</files>
  <read_first>lib/main.dart, lib/features/ingestion/services/share_service.dart</read_first>
  <action>Register BackupSchedulerService in setupLocator after SettingsService registration. Invoke scheduler bootstrap after ShareService init so startup sequence runs share listener first, then scheduler eligibility check. Ensure bootstrap call is non-blocking for app launch.</action>
  <acceptance_criteria>main.dart registers BackupSchedulerService in GetIt; main.dart invokes scheduler startup check after ShareService.init(); flutter analyze exits 0.</acceptance_criteria>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Automatic backup policy is wired to startup lifecycle without degrading launch path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local archive to cloud provider | Outbound data leaves device trust boundary. |
| scheduler environment signals | Network/power signal can be stale or spoofed by platform edge states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | I | cloud upload payload | mitigate | Upload only validated backup archives with bounded manifest fields and no secrets outside required settings keys. |
| T-11-08 | D | scheduler loop | mitigate | Enforce single-run guard and deterministic skip reasons to prevent retry storms. |
| T-11-09 | R | backup execution auditing | mitigate | Persist last success/failure timestamps and reason codes in settings service for traceability. |
</threat_model>

<verification>
- flutter test test/features/backup/google_drive_backup_provider_test.dart
- flutter test test/features/backup/backup_scheduler_service_test.dart
- flutter analyze
</verification>

<success_criteria>
- Google Drive provider seam is available through explicit contract.
- Scheduler policy runs daily with Wi-Fi and charging constraints.
- Startup wiring initializes scheduler path and keeps app startup responsive.
</success_criteria>

<output>
After completion, create .planning/phases/11-cloud-data-portability/11-03-SUMMARY.md
</output>
