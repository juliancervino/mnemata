---
phase: 19-web-parity-core-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/core/database/app_database.dart
  - lib/features/ingestion/services/share_service.dart
  - lib/features/ingestion/services/extraction_service.dart
  - lib/features/intelligence/services/ai_provider_client.dart
  - web/index.html
  - web/drift_worker.js
  - web/sqlite3.wasm
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04]
must_haves:
  truths:
    - Web startup succeeds without dart:io or dart:ffi runtime/compile failures in app initialization paths.
    - Platform-specific services resolve to web-safe behavior while mobile behavior remains unchanged.
    - Drift on web uses explicit wasm + worker assets and can open the local database deterministically.
  artifacts:
    - path: pubspec.yaml
      provides: Web parity dependency baseline (drag-drop and embedded PDF viewer libraries).
    - path: lib/main.dart
      provides: Platform-safe service registration and startup sequencing.
    - path: lib/core/database/app_database.dart
      provides: Drift web runtime configuration with deterministic web options.
    - path: web/drift_worker.js
      provides: Drift worker asset required by web sqlite runtime.
    - path: web/sqlite3.wasm
      provides: Sqlite wasm binary required by Drift web.
  key_links:
    - from: lib/main.dart
      to: lib/features/ingestion/services/share_service.dart
      via: conditional service initialization and platform guard
      pattern: kIsWeb
    - from: lib/core/database/app_database.dart
      to: web/drift_worker.js
      via: DriftWebOptions worker path
      pattern: driftDatabase\(web:
---

<objective>
Unblock Phase 19 by making the application web-safe at startup and persistence layers so parity features can be implemented and tested on Chrome.

Purpose: Prevent platform crashes and compile blockers before implementing WEB-01..WEB-04 behavior tasks.
Output: Web-safe startup wiring, Drift web runtime configuration, and required web assets/dependencies.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-web-parity-core-flows/19-CONTEXT.md
@.planning/phases/19-web-parity-core-flows/19-RESEARCH.md
@lib/main.dart
@lib/core/database/app_database.dart
@lib/features/ingestion/services/share_service.dart
@lib/features/ingestion/services/extraction_service.dart
@lib/features/intelligence/services/ai_provider_client.dart

<interfaces>
From lib/main.dart:
- Future<void> setupLocator()
- void main()

From lib/core/database/app_database.dart:
- static QueryExecutor _openConnection()
- class AppDatabase extends _$AppDatabase

From lib/features/ingestion/services/share_service.dart:
- class ShareService
- void init()
- Future<void> handleUrl(String? text)
- Future<void> handleFile(SharedMediaFile sharedFile)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make startup and core services web-safe before parity feature work</name>
  <read_first>
    - lib/main.dart
    - lib/features/ingestion/services/share_service.dart
    - lib/features/ingestion/services/extraction_service.dart
    - lib/features/intelligence/services/ai_provider_client.dart
    - .planning/phases/19-web-parity-core-flows/19-RESEARCH.md
  </read_first>
  <files>lib/main.dart, lib/features/ingestion/services/share_service.dart, lib/features/ingestion/services/extraction_service.dart, lib/features/intelligence/services/ai_provider_client.dart</files>
  <action>
    Introduce platform-safe guards and conditional implementations so web startup does not import or execute mobile-only code paths.
    Keep mobile behavior identical while removing web compile/runtime blockers for dart:io and plugin-specific startup hooks.
    Register and initialize share/extraction/network error handling using web-safe branches in setupLocator/main.
    This task is a prerequisite for D-01 through D-32 delivery because all web parity decisions require a working web runtime.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - Web startup path does not eagerly initialize mobile-only intent listeners.
    - No dart:io-dependent startup path is reachable from main on web.
    - Mobile startup and existing share flow continue to compile without behavioral regressions.
  </acceptance_criteria>
  <done>Platform-safe startup/service registration is implemented and analyzer reports no new errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Drift web runtime and add required phase dependencies/assets</name>
  <read_first>
    - pubspec.yaml
    - lib/core/database/app_database.dart
    - web/index.html
    - .planning/phases/19-web-parity-core-flows/19-RESEARCH.md
  </read_first>
  <files>pubspec.yaml, lib/core/database/app_database.dart, web/index.html, web/drift_worker.js, web/sqlite3.wasm</files>
  <action>
    Add required dependencies for Phase 19 parity implementation: flutter_dropzone for D-02 and syncfusion_flutter_pdfviewer for D-19/D-24.
    Update AppDatabase web open path to use explicit Drift web options with deterministic sqlite wasm and worker asset references.
    Place drift_worker.js and sqlite3.wasm under web/ and ensure index.html loads runtime prerequisites expected by Drift web.
    Keep non-web database behavior unchanged.
  </action>
  <verify>
    <automated>flutter pub get && flutter test --platform chrome test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Chrome test run no longer fails on missing wasm/worker configuration.
    - Database opens on web using explicit Drift web options.
    - Dependencies required by later plans are present and lockfile updates cleanly.
  </acceptance_criteria>
  <done>Web database runtime is configured and chrome smoke test passes.</done>
</task>

</tasks>

<must_haves>
<truths>
- The app can boot and render the list screen on Chrome.
- Drift opens with web assets without runtime fallback ambiguity.
- Foundation is in place for WEB-01..WEB-04 implementation plans.
</truths>
<artifacts>
- pubspec.yaml includes flutter_dropzone and syncfusion_flutter_pdfviewer.
- web/drift_worker.js and web/sqlite3.wasm exist.
- startup and core services compile for both web and mobile.
</artifacts>
<key_links>
- setupLocator/main selects platform-safe service behavior.
- AppDatabase web options reference worker/wasm assets correctly.
</key_links>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser input -> startup services | Untrusted web runtime inputs and platform capabilities cross into app startup paths. |
| Browser -> local web DB | Web worker and wasm assets execute local persistence logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | D | startup bootstrap | mitigate | Remove/guard mobile-only initialization paths on web and fail fast with explicit fallbacks. |
| T-19-01-02 | T | Drift web asset resolution | mitigate | Pin deterministic worker/wasm paths in database open options and verify via chrome test gate. |
| T-19-01-03 | I | platform exception handling | mitigate | Normalize network/platform error mapping without leaking stack traces into user-facing flows. |
</threat_model>

<verification>
- flutter analyze
- flutter test --platform chrome test/widget_test.dart
</verification>

<success_criteria>
- Web runtime is unblocked for feature plans.
- Required dependencies/assets exist and are wired.
- Mobile behavior remains unchanged while web compile/runtime blockers are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/19-web-parity-core-flows/19-01-SUMMARY.md`.
</output>
