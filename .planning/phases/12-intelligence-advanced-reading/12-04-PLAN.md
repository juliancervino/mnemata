---
phase: 12-intelligence-advanced-reading
plan: 04
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - lib/features/intelligence/services/annotation_service.dart
  - lib/features/intelligence/presentation/annotation_list_panel.dart
  - lib/features/intelligence/presentation/reader_selection_actions.dart
  - lib/features/reader/presentation/reader_screen.dart
  - test/features/intelligence/services/annotation_service_test.dart
autonomous: true
requirements:
  - POR-04
must_haves:
  truths:
    - "Reader supports selection-driven highlight or highlight+note flow."
    - "Annotations persist quote text + positional anchors and survive reader reload."
    - "Reader displays inline highlight styling and per-item annotation list with edit/delete/navigation."
    - "Deleting an item removes associated annotations/highlights."
  artifacts:
    - path: lib/features/intelligence/services/annotation_service.dart
      provides: Annotation CRUD + anchor persistence for per-item highlights.
    - path: lib/features/intelligence/presentation/annotation_list_panel.dart
      provides: Per-item annotation list UI with navigate/edit/delete actions.
    - path: lib/features/reader/presentation/reader_screen.dart
      provides: Selection action flow + inline highlight rendering + annotation list integration.
  key_links:
    - from: lib/features/reader/presentation/reader_screen.dart
      to: lib/features/intelligence/services/annotation_service.dart
      via: selection save/edit/delete callbacks.
      pattern: annotation
    - from: lib/core/database/app_database.dart
      to: lib/features/intelligence/services/annotation_service.dart
      via: item delete cascade behavior.
      pattern: delete
---

<objective>
Implement persistent highlights and annotations in Reader with anchored storage, inline render, list management, and item-delete cascade behavior.

Purpose: Deliver D-09/D-10/D-11/D-12 on top of the persistence contracts established in plan 12-01.
Output: Reader-integrated annotation UX and tested annotation service behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-intelligence-advanced-reading/12-CONTEXT.md
@.planning/phases/12-intelligence-advanced-reading/12-01-SUMMARY.md
@lib/features/reader/presentation/reader_screen.dart
@lib/core/database/app_database.dart

<interfaces>
From plan 12-01 expected contract:
```dart
class AnnotationRecord {
  final int id;
  final int itemId;
  final String quoteText;
  final String anchorJson;
  final String? note;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement annotation service and persistence-backed CRUD (D-10, D-12)</name>
  <files>lib/features/intelligence/services/annotation_service.dart, test/features/intelligence/services/annotation_service_test.dart</files>
  <behavior>
    - Test 1: Creating annotation persists itemId + quote + anchor + optional note.
    - Test 2: Reading annotations for item returns stable ordering for UI list rendering.
    - Test 3: Deleting parent item cascades and removes annotation records.
  </behavior>
  <action>Create AnnotationService over AppDatabase annotation tables from plan 12-01. Implement create/update/delete/list methods with anchor JSON schema validation and deterministic ordering for UI consumption. Verify delete-item cascade by exercising existing item deletion path and asserting annotation cleanup.</action>
  <verify>
    <automated>flutter test test/features/intelligence/services/annotation_service_test.dart</automated>
  </verify>
  <done>Annotation persistence and cascade cleanup are reliable and test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add selection-based highlight/note UX in Reader (D-09, D-11)</name>
  <files>lib/features/intelligence/presentation/annotation_list_panel.dart, lib/features/intelligence/presentation/reader_selection_actions.dart, lib/features/reader/presentation/reader_screen.dart, test/features/intelligence/services/annotation_service_test.dart</files>
  <behavior>
    - Test 1: Selected text can be saved as highlight-only or highlight+note.
    - Test 2: Reader reload preserves highlights and annotation list entries.
    - Test 3: Annotation list supports navigate/edit/delete without leaving Reader route.
  </behavior>
  <action>Add Reader selection action flow and annotation list panel integrated in existing ReaderScreen. Render inline highlights from persisted anchors and expose quick navigation/edit/delete actions. Keep implementation within current Reader route; do not add TTS controls or unrelated playback behavior (TTS deferred out of Phase 12).</action>
  <verify>
    <automated>flutter test test/features/intelligence/services/annotation_service_test.dart</automated>
  </verify>
  <done>Reader supports persistent highlights + annotation management and explicitly excludes TTS scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered content selection to persisted anchors | User-selected text offsets are converted into stored anchor data. |
| annotation metadata to reader rendering | Persisted annotation content is rendered back into reader UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | T | anchor serialization | mitigate | Validate anchor schema before save and ignore malformed anchors during render. |
| T-12-12 | D | reader highlight rendering | mitigate | Bound highlight render work per item and lazy-load annotation list to prevent UI stalls. |
| T-12-13 | I | annotation note text | accept | Notes are local-only non-sensitive user content; no external transmission path in this plan. |
</threat_model>

<verification>
- flutter test test/features/intelligence/services/annotation_service_test.dart
</verification>

<success_criteria>
- Reader supports selection-driven persistent highlights and notes.
- Per-item annotation panel supports navigation/edit/delete.
- Item deletion cascades annotation cleanup.
- TTS is not implemented in this phase.
</success_criteria>

<output>
After completion, create .planning/phases/12-intelligence-advanced-reading/12-04-SUMMARY.md
</output>
