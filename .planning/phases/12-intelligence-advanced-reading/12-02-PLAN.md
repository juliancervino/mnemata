---
phase: 12-intelligence-advanced-reading
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - lib/features/intelligence/services/summary_service.dart
  - lib/features/intelligence/services/tag_suggestion_service.dart
  - lib/features/intelligence/presentation/summary_panel.dart
  - lib/features/intelligence/presentation/tag_suggestion_sheet.dart
  - lib/features/reader/presentation/reader_screen.dart
  - test/features/intelligence/services/summary_service_test.dart
  - test/features/intelligence/services/tag_suggestion_service_test.dart
autonomous: true
requirements:
  - POR-04
must_haves:
  truths:
    - "Reader can generate on-demand summary and display fixed 3-block format."
    - "Summary generation is disabled with explicit guidance when API key is missing."
    - "Only URL items with extracted content can be summarized; unsupported items show explicit state."
    - "Tag suggestions only propose existing tags and never create new tags in this phase."
  artifacts:
    - path: lib/features/intelligence/services/summary_service.dart
      provides: On-demand summary generation with content-hash cache and idempotent reuse.
    - path: lib/features/intelligence/services/tag_suggestion_service.dart
      provides: Existing-tag-only ranking and suggestion results.
    - path: lib/features/reader/presentation/reader_screen.dart
      provides: Reader actions for summary trigger and tag suggestion apply/ignore flows.
  key_links:
    - from: lib/features/reader/presentation/reader_screen.dart
      to: lib/features/intelligence/services/summary_service.dart
      via: User-triggered summary action.
      pattern: generateSummary
    - from: lib/features/intelligence/services/tag_suggestion_service.dart
      to: lib/core/database/app_database.dart
      via: Existing labels fetch and apply path.
      pattern: watchAllLabels
---

<objective>
Deliver reader-facing AI summary and tag suggestion flows that obey API-key gating, unsupported states, idempotent caching, and existing-tag-only constraints.

Purpose: Implement D-01/D-02/D-03/D-04/D-13/D-14/D-15/D-20 with no scope creep into new-tag creation.
Output: Reader-integrated summary and tag suggestion UX backed by tested intelligence services.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-intelligence-advanced-reading/12-CONTEXT.md
@.planning/phases/12-intelligence-advanced-reading/12-01-SUMMARY.md
@lib/features/reader/presentation/reader_screen.dart
@lib/core/database/app_database.dart
@lib/features/settings/services/settings_service.dart

<interfaces>
From plan 12-01 expected contracts:
```dart
abstract class ApiKeyStore {
  Future<void> saveKey(String apiKey);
  Future<String?> readKey();
  Future<void> clearKey();
}

enum IntelligenceErrorCode { missingApiKey, timeout, authenticationFailed, rateLimited, providerUnavailable, invalidResponse }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement summary service with strict gating and idempotent cache (D-01, D-02, D-03, D-04, D-20)</name>
  <files>lib/features/intelligence/services/summary_service.dart, lib/features/intelligence/presentation/summary_panel.dart, lib/features/reader/presentation/reader_screen.dart, test/features/intelligence/services/summary_service_test.dart</files>
  <behavior>
    - Test 1: Missing API key returns missingApiKey error and UI setup guidance.
    - Test 2: File items or URL items without extracted content return unsupported-state result.
    - Test 3: Unchanged content hash reuses cached summary; changed content hash triggers regeneration.
    - Test 4: Output always contains TL;DR, Key Points (3-5 bullets), and Why it matters blocks.
  </behavior>
  <action>Create SummaryService that reads key from ApiKeyStore, validates item eligibility, computes content hash, and reuses cached summaries when unchanged. Build summary panel widget and ReaderScreen action to trigger generation on demand and render fixed-format blocks. Show explicit unsupported and missing-key states instead of silent failure.</action>
  <verify>
    <automated>flutter test test/features/intelligence/services/summary_service_test.dart</automated>
  </verify>
  <done>Reader can request summaries on demand with deterministic eligibility checks and content-hash idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement existing-tag-only AI suggestion flow in Reader (D-13, D-14, D-15)</name>
  <files>lib/features/intelligence/services/tag_suggestion_service.dart, lib/features/intelligence/presentation/tag_suggestion_sheet.dart, lib/features/reader/presentation/reader_screen.dart, test/features/intelligence/services/tag_suggestion_service_test.dart</files>
  <behavior>
    - Test 1: Suggestion prompt/input uses extracted content + title context plus current existing tags list.
    - Test 2: Returned suggestions are filtered to existing tag IDs/names only.
    - Test 3: UI actions only allow apply, ignore, or manual existing-tag edit; no create-tag path exists.
  </behavior>
  <action>Add TagSuggestionService that retrieves existing labels from AppDatabase and asks provider for ranked matches constrained to that set. Build tag suggestion sheet from Reader actions and wire apply/ignore/manual-edit existing tags behavior only. Ensure missing API key and provider errors map to explicit user feedback.</action>
  <verify>
    <automated>flutter test test/features/intelligence/services/tag_suggestion_service_test.dart</automated>
  </verify>
  <done>AI tag suggestions are available in Reader and never propose or create new tags in Phase 12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader UI to AI service layer | User-triggered content requests cross into external provider path. |
| AI output to tag assignment | Untrusted model output could attempt non-existing or unsafe tag actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | T | summary response structure | mitigate | Validate response into strict TL;DR / key points / why-it-matters schema before render. |
| T-12-06 | E | tag suggestion apply flow | mitigate | Intersect model suggestions with DB existing tags and reject non-matching values. |
| T-12-07 | I | reader content outbound request | mitigate | Send only required fields (title/content excerpt) and never include credentials in request body. |
</threat_model>

<verification>
- flutter test test/features/intelligence/services/summary_service_test.dart
- flutter test test/features/intelligence/services/tag_suggestion_service_test.dart
</verification>

<success_criteria>
- Summary is on-demand, fixed format, idempotent by content hash, and properly gated by API key.
- Unsupported summary states are explicit for non-eligible items.
- Tag suggestions only surface existing tags and never create new tags.
</success_criteria>

<output>
After completion, create .planning/phases/12-intelligence-advanced-reading/12-02-SUMMARY.md
</output>
