---
phase: 12-intelligence-advanced-reading
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - lib/features/intelligence/services/semantic_indexer_service.dart
  - lib/features/intelligence/services/semantic_search_service.dart
  - lib/features/intelligence/presentation/semantic_mode_toggle.dart
  - lib/features/chronological_list/presentation/item_list_screen.dart
  - test/features/intelligence/services/semantic_search_service_test.dart
  - test/features/intelligence/services/semantic_indexer_service_test.dart
autonomous: true
requirements:
  - POR-04
must_haves:
  truths:
    - "Main list keeps a single search entry point with keyword/semantic mode toggle."
    - "Semantic mode requires API key; keyword mode remains fully available without key."
    - "Semantic retrieval falls back to keyword results when semantic recall is weak or unavailable."
    - "Semantic indexing runs asynchronously after save/update and never blocks ingestion/reader rendering."
  artifacts:
    - path: lib/features/intelligence/services/semantic_search_service.dart
      provides: Hybrid semantic+keyword retrieval with deterministic fallback.
    - path: lib/features/intelligence/services/semantic_indexer_service.dart
      provides: Background incremental indexing pipeline.
    - path: lib/features/chronological_list/presentation/item_list_screen.dart
      provides: Search mode toggle and stream selection wiring.
  key_links:
    - from: lib/features/chronological_list/presentation/item_list_screen.dart
      to: lib/features/intelligence/services/semantic_search_service.dart
      via: semantic mode query path.
      pattern: semantic
    - from: lib/features/ingestion/services/share_service.dart
      to: lib/features/intelligence/services/semantic_indexer_service.dart
      via: post-save/update indexing trigger.
      pattern: enqueue
---

<objective>
Add hybrid semantic search to the existing list search entry point, including API-key gating, keyword fallback, and async incremental indexing.

Purpose: Implement D-05/D-06/D-07/D-08/D-21 while preserving current fast local keyword behavior.
Output: Semantic search services and list-screen mode wiring with automated verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-intelligence-advanced-reading/12-CONTEXT.md
@.planning/phases/12-intelligence-advanced-reading/12-01-SUMMARY.md
@lib/features/chronological_list/presentation/item_list_screen.dart
@lib/features/ingestion/services/share_service.dart
@lib/core/database/app_database.dart

<interfaces>
From lib/features/chronological_list/presentation/item_list_screen.dart:
```dart
Stream<List<MnemataItem>> _getStream(AppDatabase database) {
  if (_searchQuery.isNotEmpty) {
    return database.searchItems(_searchQuery, labelIds: _selectedLabelIds.toList());
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement semantic indexer and hybrid retrieval services (D-06, D-07, D-08)</name>
  <files>lib/features/intelligence/services/semantic_indexer_service.dart, lib/features/intelligence/services/semantic_search_service.dart, test/features/intelligence/services/semantic_indexer_service_test.dart, test/features/intelligence/services/semantic_search_service_test.dart</files>
  <behavior>
    - Test 1: Indexer schedules/upserts semantic chunks asynchronously after content save/update.
    - Test 2: Semantic search returns concept-near matches when index + provider are available.
    - Test 3: If index/provider is missing or recall below threshold, service falls back to keyword search results.
  </behavior>
  <action>Create SemanticIndexerService for incremental non-blocking indexing and SemanticSearchService for hybrid query execution. Limit semantic indexing scope to extracted article content, keep title/url keyword matching in existing path, and return merged ordered results with explicit fallback reason metadata for UX messaging.</action>
  <verify>
    <automated>flutter test test/features/intelligence/services/semantic_indexer_service_test.dart test/features/intelligence/services/semantic_search_service_test.dart</automated>
  </verify>
  <done>Semantic indexing/search services exist with deterministic fallback to keyword behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire list-screen search mode toggle with API-key gating (D-05, D-21)</name>
  <files>lib/features/intelligence/presentation/semantic_mode_toggle.dart, lib/features/chronological_list/presentation/item_list_screen.dart, test/features/intelligence/services/semantic_search_service_test.dart</files>
  <behavior>
    - Test 1: Existing single search entry point supports keyword/semantic mode toggle.
    - Test 2: Without API key, semantic mode is disabled and keyword mode continues unchanged.
    - Test 3: Semantic mode displays explanatory unavailable state when capability requirements are unmet.
  </behavior>
  <action>Add semantic mode toggle control in the existing ItemListScreen search UI and route semantic queries through SemanticSearchService only when key + toggle conditions are met. Preserve current database.searchItems behavior as default keyword path and for fallback. Ensure no additional search screen/route is introduced.</action>
  <verify>
    <automated>flutter test test/features/intelligence/services/semantic_search_service_test.dart</automated>
  </verify>
  <done>Search UX remains single-entry and gains gated semantic mode without regressing keyword search.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| list search input to semantic retrieval | User text enters semantic/provider processing path. |
| ingestion save/update to background indexer | Content updates trigger asynchronous indexing jobs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | D | semantic query pipeline | mitigate | Enforce bounded latency + fallback to local keyword search on timeout/unavailable conditions. |
| T-12-09 | T | semantic score interpretation | mitigate | Apply deterministic threshold rules before accepting semantic-only ranking. |
| T-12-10 | R | mode gating behavior | mitigate | Log/search-state metadata in memory and show explicit mode/fallback indicator to user. |
</threat_model>

<verification>
- flutter test test/features/intelligence/services/semantic_indexer_service_test.dart
- flutter test test/features/intelligence/services/semantic_search_service_test.dart
</verification>

<success_criteria>
- Single search entry point supports keyword/semantic modes.
- Semantic mode is key-gated and degrades gracefully to keyword results.
- Indexing is asynchronous and does not block ingestion or reader usage.
</success_criteria>

<output>
After completion, create .planning/phases/12-intelligence-advanced-reading/12-03-SUMMARY.md
</output>
