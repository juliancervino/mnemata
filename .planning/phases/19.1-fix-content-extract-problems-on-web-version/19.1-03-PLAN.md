---
phase: 19.1
plan: 03
type: execute
wave: 3
depends_on: ["19.1-01", "19.1-02"]
files_modified: [lib/features/ingestion/services/pdf_extraction_service.dart, lib/features/ingestion/services/share_service.dart, lib/features/ingestion/presentation/ingestion_failure_actions_sheet.dart, lib/features/ingestion/presentation/manual_ingest_dialog.dart]
autonomous: false
requirements: [WEB-05]
must_haves:
  truths:
    - "PDF extraction works on web using byte buffers"
    - "Users can paste HTML/Text manually via a dialog"
    - "IngestionFailureActionsSheet offers the manual ingest path"
  artifacts:
    - path: "lib/features/ingestion/presentation/manual_ingest_dialog.dart"
      provides: "Manual content fallback UI"
    - path: "lib/features/ingestion/services/pdf_extraction_service.dart"
      provides: "Platform-agnostic PDF extraction"
  key_links:
    - from: "IngestionFailureActionsSheet"
      to: "ManualIngestDialog"
      via: "Navigator.push"
---

<objective>
Fix web PDF extraction and implement Manual Ingest fallback to ensure 100% ingestion coverage.

Purpose: Remove platform-specific crashes for PDFs and provide a "human-in-the-loop" fallback for automated extraction failures.
Output: Working PDF ingestion on web and a manual paste flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-RESEARCH.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-02-SUMMARY.md
@lib/features/ingestion/services/pdf_extraction_service.dart
@lib/features/ingestion/services/share_service.dart
@lib/features/ingestion/presentation/ingestion_failure_actions_sheet.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PDF extraction for web</name>
  <files>lib/features/ingestion/services/pdf_extraction_service.dart, lib/features/ingestion/services/share_service.dart</files>
  <action>
    - Refactor `PdfExtractionService.extractText` to accept `Uint8List bytes` instead of a file path.
    - Remove `dart:io` usage from `PdfExtractionService`.
    - Update `ShareService._handleFile` (IO) to read bytes before calling the service.
    - Update `ShareService.handleWebFile` to attempt PDF extraction if the file type matches, passing the existing `bytes`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/ingestion/services/pdf_extraction_service.dart</automated>
  </verify>
  <done>PDF extraction works on both mobile and web platforms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Manual Ingest UI and logic</name>
  <files>lib/features/ingestion/presentation/manual_ingest_dialog.dart, lib/features/ingestion/presentation/ingestion_failure_actions_sheet.dart, lib/features/ingestion/services/share_service.dart</files>
  <action>
    - Create `ManualIngestDialog` with a `TextField` (multiline) for pasting HTML or Text.
    - Add `manualIngest` option to `IngestionFailureAction` enum and `IngestionFailureActionsSheet`.
    - In `ShareService._handleUrl`, when `manualIngest` action is received:
      1. Show `ManualIngestDialog`.
      2. If HTML is pasted, run it through `ExtractionService` (which uses Readability).
      3. Push the `IngestionSummaryScreen` with the results.
  </action>
  <verify>
    <automated>flutter analyze lib/features/ingestion/presentation/manual_ingest_dialog.dart</automated>
  </verify>
  <done>Users can manually paste content when automated extraction fails.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify tiered extraction and manual fallback on web</name>
  <action>Human verification of web extraction and manual ingest flow.</action>
  <what-built>Tiered extraction and manual fallback on web.</what-built>
  <how-to-verify>
    1. Run Mnemata on Web.
    2. Try to ingest a URL that normally has CORS issues (e.g., a news site). Verify it succeeds via proxy.
    3. Try to ingest a PDF file. Verify content is extracted.
    4. Simulate a failed extraction (e.g., by using an invalid URL) and verify the "Paste Content Manually" option appears and works.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <done>Verification complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → ManualIngestDialog | User-provided HTML is pasted into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-03 | Tampering | Manual Ingest | mitigate | Sanitize user-pasted HTML before processing/rendering. |
</threat_model>

<verification>
Manual test of the "Paste Content Manually" flow on web.
</verification>

<success_criteria>
User can successfully ingest a PDF file on web and manually paste HTML for a failed URL extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-03-SUMMARY.md`
</output>
