---
phase: 19.1
plan: 01
type: execute
wave: 1
depends_on: ["19.1-00"]
files_modified: [web/defuddle.js, web/index.html, lib/features/ingestion/services/readability_platform_web.dart, lib/features/ingestion/services/readability_platform.dart, lib/features/ingestion/services/metadata_extraction_service.dart]
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "defuddle.js is loaded in the browser context"
    - "Content distillation can be triggered from Dart on web using defuddle"
    - "Metadata extraction prioritizes JSON-LD and OpenGraph before fallbacks"
  artifacts:
    - path: "web/defuddle.js"
      provides: "Defuddle distillation library"
    - path: "lib/features/ingestion/services/readability_platform_web.dart"
      provides: "JS-interop wrapper for defuddle"
    - path: "lib/features/ingestion/services/metadata_extraction_service.dart"
      provides: "Priority-based metadata extraction logic"
  key_links:
    - from: "lib/features/ingestion/services/readability_platform_web.dart"
      to: "web/defuddle.js"
      via: "dart:js_interop"
---

<objective>
Implement the web foundation for content distillation by wrapping 'defuddle' (as used in Obsidian Web Clipper) and implementing a metadata priority cascade.

Purpose: Enable high-fidelity client-side HTML to Markdown conversion and robust metadata extraction on web.
Output: Working defuddle distillation and prioritized metadata parsing on web.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-RESEARCH.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-00-SUMMARY.md
@lib/features/ingestion/services/readability_platform.dart
@lib/features/ingestion/services/readability_platform_stub.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add defuddle.js to web project</name>
  <files>web/defuddle.js, web/index.html</files>
  <action>
    - Download defuddle.js (from https://cdn.jsdelivr.net/npm/defuddle@1.x/dist/defuddle.js) and save to web/defuddle.js.
    - Add script tag to web/index.html before the main flutter script: `<script src="defuddle.js"></script>`.
  </action>
  <verify>
    <automated>ls web/defuddle.js && grep "defuddle.js" web/index.html</automated>
  </verify>
  <done>defuddle.js is available to the web runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JS-interop wrapper and metadata priority</name>
  <files>lib/features/ingestion/services/readability_platform_web.dart, lib/features/ingestion/services/readability_platform.dart, lib/features/ingestion/services/metadata_extraction_service.dart</files>
  <action>
    - Create lib/features/ingestion/services/readability_platform_web.dart using `dart:js_interop` and `package:web`.
    - Define `@JS('Defuddle')` extension type wrapping the JS constructor and `parse()` method.
    - Create `MetadataExtractionService` (or update existing if applicable) to implement the metadata cascade:
      1. JSON-LD (`<script type="application/ld+json">`) extraction for Title, Author, Date.
      2. OpenGraph/Twitter Meta Tags.
      3. DOM heuristics (H1, etc. - can be provided by defuddle output).
    - Update lib/features/ingestion/services/readability_platform.dart to conditionally export 'readability_platform_web.dart'.
  </action>
  <verify>
    <automated>flutter test test/features/ingestion/services/metadata_priority_test.dart</automated>
  </verify>
  <done>Dart can call defuddle on web and metadata is extracted with correct priority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart → JS Interop | External library (defuddle.js) receives HTML strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Tampering | defuddle.js output | mitigate | Sanitize distilled content in the Reader component before rendering. |
</threat_model>

<verification>
Run `flutter test test/features/ingestion/services/metadata_priority_test.dart` to verify logic.
</verification>

<success_criteria>
Article parsing on web returns valid title/content from defuddle, and metadata extraction prefers JSON-LD when present.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-01-SUMMARY.md`
</output>
