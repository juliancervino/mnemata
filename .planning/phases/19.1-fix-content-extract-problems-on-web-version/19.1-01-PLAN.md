---
phase: 19.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/Readability.js, web/index.html, lib/features/ingestion/services/readability_platform_web.dart, lib/features/ingestion/services/readability_platform.dart]
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "Readability.js is loaded in the browser context"
    - "Readability distillation can be triggered from Dart on web"
  artifacts:
    - path: "web/Readability.js"
      provides: "Mozilla Readability library"
    - path: "lib/features/ingestion/services/readability_platform_web.dart"
      provides: "JS-interop wrapper for Readability"
  key_links:
    - from: "lib/features/ingestion/services/readability_platform_web.dart"
      to: "web/Readability.js"
      via: "dart:js_interop"
---

<objective>
Implement the web foundation for content distillation by wrapping @mozilla/readability using JS-interop.

Purpose: Enable client-side HTML to Markdown/Text conversion on the web platform, replacing the current null stub.
Output: Working Readability distillation on web.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-RESEARCH.md
@lib/features/ingestion/services/readability_platform.dart
@lib/features/ingestion/services/readability_platform_stub.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Readability.js to web project</name>
  <files>web/Readability.js, web/index.html</files>
  <action>
    - Download @mozilla/readability v0.5.0 (https://cdn.jsdelivr.net/npm/@mozilla/readability@0.5.0/Readability.js) and save to web/Readability.js.
    - Add script tag to web/index.html before the main flutter script: `<script src="Readability.js"></script>`.
  </action>
  <verify>
    <automated>ls web/Readability.js && grep "Readability.js" web/index.html</automated>
  </verify>
  <done>Readability.js is available to the web runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JS-interop wrapper</name>
  <files>lib/features/ingestion/services/readability_platform_web.dart, lib/features/ingestion/services/readability_platform.dart</files>
  <action>
    - Create lib/features/ingestion/services/readability_platform_web.dart using `dart:js_interop` and `package:web`.
    - Define `@JS('Readability')` extension type wrapping the JS constructor.
    - Implement `Article? parseAsync(String url)` (fetch HTML then parse) and `Article? parseHtmlDocument(String html)`.
    - Update lib/features/ingestion/services/readability_platform.dart to export 'readability_platform_web.dart' when `dart.library.js_interop` is available.
    - Ensure the Article type matches the existing definition in lib/features/ingestion/services/readability_platform_io.dart.
  </action>
  <verify>
    <automated>flutter analyze lib/features/ingestion/services/readability_platform.dart</automated>
  </verify>
  <done>Dart can call Readability.js on web to distill content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart → JS Interop | External library (Readability.js) receives HTML strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Tampering | Readability.js output | mitigate | Sanitize distilled HTML in the Reader component before rendering (per existing Reader implementation). |
</threat_model>

<verification>
Check if `flutter build web` succeeds with the new interop.
</verification>

<success_criteria>
`Article? parseHtmlDocument(String html)` returns a non-null result for a sample HTML string when running in a web environment.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-01-SUMMARY.md`
</output>
