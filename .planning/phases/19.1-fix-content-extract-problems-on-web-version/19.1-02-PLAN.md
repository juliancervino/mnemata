---
phase: 19.1
plan: 02
type: execute
wave: 2
depends_on: ["19.1-01"]
files_modified: [lib/features/ingestion/services/extraction_service.dart]
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "ExtractionService uses corsproxy.io as the primary raw HTML proxy"
    - "ExtractionService uses allorigins.win as a fallback raw HTML proxy"
    - "ExtractionService applies client-side distillation (defuddle) to raw HTML fetched via proxies"
    - "Jina reader proxy is removed from the codebase"
  artifacts:
    - path: "lib/features/ingestion/services/extraction_service.dart"
      provides: "Tiered raw HTML proxy pipeline"
  key_links:
    - from: "ExtractionService"
      to: "corsproxy.io"
      via: "http GET"
    - from: "ExtractionService"
      to: "allorigins.win"
      via: "http GET"
---

<objective>
Implement the tiered raw proxy extraction pipeline in ExtractionService to bypass CORS using only raw HTML proxies and client-side distillation.

Purpose: Provide reliable URL ingestion on web without external reader dependencies, maintaining privacy and control.
Output: Robust URL extraction logic using raw CORS proxies.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-RESEARCH.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-01-SUMMARY.md
@lib/features/ingestion/services/extraction_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tiered raw proxy pipeline</name>
  <files>lib/features/ingestion/services/extraction_service.dart</files>
  <action>
    - **ELIMINATE** `r.jina.ai` proxy usage in `_extractFromReadableProxy`.
    - Refactor `extractContent` to implement the following sequence on web:
      1. Direct fetch (may fail due to CORS).
      2. Fetch raw HTML via `corsproxy.io` (https://corsproxy.io/?{url}).
      3. Fetch raw HTML via `allorigins.win` (https://api.allorigins.win/get?url={url}).
      4. For any raw HTML fetched, use the `MetadataExtractionService` (from Plan 01) and `ReadabilityWrapper.parseHtml` (using defuddle).
    - Implement robust error handling for proxy timeouts and 403/429 status codes.
  </action>
  <verify>
    <automated>flutter test test/features/ingestion/services/extraction_service_test.dart</automated>
  </verify>
  <done>ExtractionService uses raw proxies and client-side parsing exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Add failure heuristics</name>
  <files>lib/features/ingestion/services/extraction_service.dart</files>
  <action>
    - Add logic to detect common "blocked" states (e.g., 403 Forbidden, 429 Too Many Requests, or HTML containing "Cloudflare", "Access Denied").
    - Return a specific error state or custom exception that can be used by the UI to trigger the "Manual Ingest" path.
  </action>
  <verify>
    <automated>flutter test test/features/ingestion/services/extraction_service_test.dart</automated>
  </verify>
  <done>ExtractionService identifies when a site is actively blocking automated scrapers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExtractionService → External Proxies | Outgoing requests to corsproxy.io and allorigins.win |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-02 | Information Disclosure | URL Extraction | accept | Only public URLs are sent to proxies. |
</threat_model>

<verification>
Unit tests for `ExtractionService` verify the sequence of calls and handling of proxy responses.
</verification>

<success_criteria>
`extractContent` successfully returns clean content for a CORS-blocked URL by falling back to `corsproxy.io` or `allorigins.win` followed by client-side distillation.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-02-SUMMARY.md`
</output>
