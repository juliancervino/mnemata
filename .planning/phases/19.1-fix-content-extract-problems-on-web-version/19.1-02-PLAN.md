---
phase: 19.1
plan: 02
type: execute
wave: 2
depends_on: ["19.1-01"]
files_modified: [lib/features/ingestion/services/extraction_service.dart]
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "ExtractionService uses r.jina.ai as the primary proxy"
    - "ExtractionService uses allorigins.win as a raw HTML fallback"
    - "ExtractionService applies Readability distillation to raw HTML on web"
  artifacts:
    - path: "lib/features/ingestion/services/extraction_service.dart"
      provides: "Tiered extraction pipeline"
  key_links:
    - from: "ExtractionService"
      to: "r.jina.ai"
      via: "http GET"
    - from: "ExtractionService"
      to: "allorigins.win"
      via: "http GET"
---

<objective>
Implement the tiered extraction pipeline in ExtractionService to bypass CORS and improve content quality on web.

Purpose: Provide reliable URL ingestion on web using a sequence of specialized proxies and client-side distillation.
Output: Robust URL extraction logic in ExtractionService.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-RESEARCH.md
@.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-01-SUMMARY.md
@lib/features/ingestion/services/extraction_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tiered extraction pipeline</name>
  <files>lib/features/ingestion/services/extraction_service.dart</files>
  <action>
    - Refactor `extractContent` to implement the following sequence on web:
      1. Direct metadata fetch (already exists).
      2. `r.jina.ai` proxy (returns clean markdown/text).
      3. `allorigins.win` proxy (returns raw HTML).
      4. If raw HTML is returned from (3), run it through `ReadabilityWrapper.parseHtml`.
    - Ensure `_extractFromReadableProxy` is updated to handle `r.jina.ai` more robustly.
    - Implement `_extractFromRawProxy` for `allorigins.win`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/ingestion/services/extraction_service.dart</automated>
  </verify>
  <done>ExtractionService attempts multiple proxy fallbacks on web.</done>
</task>

<task type="auto">
  <name>Task 2: Add failure heuristics</name>
  <files>lib/features/ingestion/services/extraction_service.dart</files>
  <action>
    - Add logic to detect common "blocked" states (e.g., 403, 429 status codes, or content containing "Cloudflare", "Captcha").
    - Return specific indicators or error codes that can be used by the UI to show tailored messages (per D-05).
  </action>
  <verify>
    <automated>flutter test test/features/ingestion/services/extraction_service_test.dart</automated>
  </verify>
  <done>ExtractionService can identify when extraction is blocked by scrapers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExtractionService → External Proxies | Outgoing requests to jina.ai and allorigins.win |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-02 | Information Disclosure | URL Extraction | accept | Only public URLs are sent to proxies; user credentials are not included in extraction requests. |
</threat_model>

<verification>
Unit tests for `ExtractionService` verify the sequence of calls and handling of proxy responses.
</verification>

<success_criteria>
`extractContent` successfully returns clean content for a CORS-blocked URL by falling back to `r.jina.ai` or `allorigins.win`.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-fix-content-extract-problems-on-web-version/19.1-02-SUMMARY.md`
</output>
