---
phase: 05-file-intelligence
plan: 01
type: execute
wave: 1
depends_on: [01-03, 02-01]
files_modified: [pubspec.yaml, lib/features/ingestion/services/pdf_extraction_service.dart, lib/features/ingestion/services/share_service.dart]
autonomous: true
requirements: [ING-04, CON-03]
must_haves:
  truths:
    - "PDF text content is extracted during ingestion"
    - "Extracted PDF text is saved to the content column in the database"
    - "PDF items are discoverable via the main search bar"
  artifacts:
    - path: "lib/features/ingestion/services/pdf_extraction_service.dart"
      provides: "PDF text extraction logic"
---

<objective>
Enable deep search for PDF documents by extracting and indexing their text content.

Purpose: Make local documents as discoverable as saved web articles.
Output: Searchable PDF items in the chronological list.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/app_database.dart
@lib/features/ingestion/services/share_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add syncfusion_flutter_pdf dependency</name>
  <files>pubspec.yaml</files>
  <action>
    Add `syncfusion_flutter_pdf: ^21.1.35` (or latest compatible) to `pubspec.yaml`.
    Run `flutter pub get`.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>PDF extraction library is available</done>
</task>

<task type="auto">
  <name>Task 2: Implement PdfExtractionService</name>
  <files>lib/features/ingestion/services/pdf_extraction_service.dart</files>
  <action>
    Create `lib/features/ingestion/services/pdf_extraction_service.dart`.
    - Implement `extractText(String filePath)` using `PdfTextExtractor`.
    - Handle potential errors (corrupt files, password protection) gracefully by returning an empty string or null.
  </action>
  <verify>
    <manual>Unit test or manual check with a sample PDF</manual>
  </verify>
  <done>Text can be extracted from PDF files</done>
</task>

<task type="auto">
  <name>Task 3: Integrate PDF Indexing into ShareService</name>
  <files>lib/features/ingestion/services/share_service.dart</files>
  <action>
    Update `ShareService` to:
    - Depend on `PdfExtractionService`.
    - In `handleFile`, if the file is a PDF, trigger text extraction.
    - Save the extracted text into the `content` field of the `mnemata_items` table.
  </action>
  <verify>
    <manual>Share a PDF, then search for a unique word inside it. The PDF should appear in results.</manual>
  </verify>
  <done>Shared PDFs are automatically indexed for search</done>
</task>

</tasks>

<verification>
Verify that searching for text contained within a PDF correctly returns that PDF item in the list.
</verification>

<success_criteria>
- PDF files shared to the app have their text extracted.
- Searching for PDF content works in the main UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-file-intelligence/05-01-SUMMARY.md`
</output>
