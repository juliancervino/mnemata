---
phase: 03-organization-continuity
plan: 01
type: execute
wave: 1
depends_on: [02-03]
files_modified: [lib/core/database/tables.dart, lib/core/database/app_database.dart, lib/features/chronological_list/presentation/item_list_screen.dart]
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03]
must_haves:
  truths:
    - "User can create and rename labels (folders/tags)"
    - "Labels can be nested (hierarchical structure)"
    - "User can assign one or more labels to an item"
    - "App tracks when an item was last opened for history view"
  artifacts:
    - path: "lib/core/database/tables.dart"
      provides: "Labels and ItemLabels tables"
    - path: "lib/core/database/app_database.dart"
      provides: "CRUD operations for labels and assignments"
  key_links:
    - from: "MnemataItem"
      to: "Labels"
      via: "ItemLabels junction table"
---

<objective>
Establish the foundational data structures and basic UI for organization (Labels/Folders) and history tracking.

Purpose: Enable users to group and categorize their saved content beyond a simple chronological list.
Output: Schema updates, basic Label management, and History tracking.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/tables.dart
@lib/core/database/app_database.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Database Schema</name>
  <files>lib/core/database/tables.dart, lib/core/database/app_database.dart</files>
  <action>
    Define the `Labels` and `ItemLabels` tables in `tables.dart`.
    - `Labels`: id, name, color (optional), parent_id (nullable, for hierarchy), is_folder (boolean).
    - `ItemLabels`: item_id, label_id.
    - Add `last_opened_at` (dateTime, nullable) to `MnemataItems`.
    - Update `AppDatabase` to version 3 and handle the migration.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Schema supports labels, nesting, and history</done>
</task>

<task type="auto">
  <name>Task 2: Implement Label CRUD in Database</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
    Add methods to `AppDatabase` for:
    - Creating/Updating/Deleting labels.
    - Assigning/Removing labels from items.
    - Watching labels and their hierarchy.
    - Updating `last_opened_at` when an item is accessed.
  </action>
  <verify>
    <automated>flutter test test/core/database/app_database_test.dart</automated>
  </verify>
  <done>Database provides full support for label management</done>
</task>

<task type="auto">
  <name>Task 3: Basic Label Management UI</name>
  <files>lib/features/organization/presentation/label_manager_screen.dart</files>
  <action>
    Create a screen to list and manage labels (folders and tags).
    - Simple list view of existing labels.
    - Ability to add new labels.
    - This serves as the foundation for the upcoming sidebar/navigation drawer.
  </action>
  <verify>
    <manual>Launch app and navigate to Label Manager (placeholder access needed)</manual>
  </verify>
  <done>Labels can be managed manually</done>
</task>

<task type="auto">
  <name>Task 4: Implement Reading History tracking</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Update `_handleOpen` in `ItemListScreen` (and any other open triggers) to update the `last_opened_at` field in the database.
  </action>
  <verify>
    <automated>Check database entry after opening an item in tests</automated>
  </verify>
  <done>Opening an item updates its history timestamp</done>
</task>

</tasks>

<verification>
Ensure that labels are persisted correctly and that hierarchical relationships are maintained.
</verification>

<success_criteria>
- Labels can be created with optional parent relationships.
- Items can be associated with multiple labels.
- Opening an item updates its 'last_opened_at' timestamp.
</success_criteria>

<output>
After completion, create `.planning/phases/03-organization-continuity/03-01-SUMMARY.md`
</output>
