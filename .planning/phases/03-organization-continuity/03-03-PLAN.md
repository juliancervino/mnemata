---
phase: 03-organization-continuity
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [pubspec.yaml, lib/features/chronological_list/presentation/item_list_screen.dart, lib/core/database/app_database.dart]
autonomous: true
requirements: [ORG-03, ORG-04]
must_haves:
  truths:
    - "User can swipe an item left to trigger a Share action"
    - "User can swipe an item right to trigger a Delete action"
    - "User can access a 'Recently Opened' view showing the last 20 items"
  artifacts:
    - path: "lib/features/chronological_list/presentation/item_list_screen.dart"
      provides: "Slidable list items"
    - path: "lib/core/database/app_database.dart"
      provides: "watchRecentlyOpened method"
---

<objective>
Implement swipe gestures for common actions and a history view for quick access to recent content.

Purpose: Improve UX by providing fast actions on list items and a dedicated view for recently accessed content.
Output: Slidable list items and a History view.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/app_database.dart
@lib/features/chronological_list/presentation/item_list_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flutter_slidable dependency</name>
  <files>pubspec.yaml</files>
  <action>
    Add `flutter_slidable: ^3.0.1` to `pubspec.yaml` and run `flutter pub get`.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>Dependency is available</done>
</task>

<task type="auto">
  <name>Task 2: Implement watchRecentlyOpened in Database</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
    Add `watchRecentlyOpened(int limit)` to `AppDatabase`.
    - Query `mnemata_items` where `last_opened_at` is not null.
    - Order by `last_opened_at` descending.
    - Limit to the specified number (default 20).
  </action>
  <verify>
    <automated>Add unit test to verify recent items are returned in correct order</automated>
  </verify>
  <done>Database supports history tracking</done>
</task>

<task type="auto">
  <name>Task 3: Implement Swipe Gestures in ItemListScreen</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Wrap `_ItemTile` content in a `Slidable` widget.
    - `startActionPane` (right swipe): Delete action.
    - `endActionPane` (left swipe): Share action (using `share_plus` or similar, or just a placeholder if not added yet).
    - Note: Need to add `share_plus` to `pubspec.yaml` if not present.
  </action>
  <verify>
    <manual>Swipe an item in the list and verify actions appear and work</manual>
  </verify>
  <done>List items support swipe actions</done>
</task>

<task type="auto">
  <name>Task 4: Add History section to Navigation Drawer</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Update the `Drawer` to include a "Recently Opened" option.
    - Selecting this option updates the filter state to show history.
    - Update `_getStream` to use `database.watchRecentlyOpened(20)` when history is selected.
  </action>
  <verify>
    <manual>Select 'Recently Opened' in the drawer and verify it shows items in order of access</manual>
  </verify>
  <done>History view is accessible via the sidebar</done>
</task>

</tasks>

<verification>
Verify that swiping works smoothly and that the history view correctly reflects the last accessed items.
</verification>

<success_criteria>
- Swipe actions (Delete/Share) are functional.
- 'Recently Opened' view shows items ordered by their last access time.
- All Phase 3 requirements (ORG-01 to ORG-04) are fulfilled.
</success_criteria>

<output>
After completion, create `.planning/phases/03-organization-continuity/03-03-SUMMARY.md`
</output>
