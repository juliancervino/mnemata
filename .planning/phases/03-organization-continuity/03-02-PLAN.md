---
phase: 03-organization-continuity
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [lib/features/chronological_list/presentation/item_list_screen.dart, lib/features/organization/presentation/label_selector_sheet.dart]
autonomous: true
requirements: [ORG-01, ORG-02]
must_haves:
  truths:
    - "User can open a sidebar to filter the list by label or folder"
    - "User can assign/remove labels for an item via a bottom sheet"
    - "The list correctly filters items based on the selected label"
  artifacts:
    - path: "lib/features/organization/presentation/label_selector_sheet.dart"
      provides: "UI for assigning labels to an item"
    - path: "lib/features/chronological_list/presentation/item_list_screen.dart"
      provides: "Drawer-based filtering and navigation"
---

<objective>
Implement the UI for assigning labels to items and the navigation/filtering system.

Purpose: Allow users to actually organize their items into the categories (labels/folders) created in the previous plan.
Output: Sidebar navigation and item-label assignment UI.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/core/database/app_database.dart
@lib/features/chronological_list/presentation/item_list_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Label Selector Bottom Sheet</name>
  <files>lib/features/organization/presentation/label_selector_sheet.dart</files>
  <action>
    Create `lib/features/organization/presentation/label_selector_sheet.dart`.
    - Display a list of all available labels with checkboxes.
    - Fetch currently assigned labels for the item and check them.
    - Handle toggling: call `assignLabelToItem` or `removeLabelFromItem`.
  </action>
  <verify>
    <manual>Open the sheet for an item, check a label, close, and verify assignment in DB</manual>
  </verify>
  <done>Labels can be assigned to items via a bottom sheet</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Label Assignment into ItemListScreen</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Add a "Label" button or long-press action to `_ItemTile` to trigger the `LabelSelectorSheet`.
    - For now, a small icon button in the tile is sufficient since swipe gestures are for Task 3.
  </action>
  <verify>
    <manual>Tap the label icon on a tile and see the bottom sheet</manual>
  </verify>
  <done>User can trigger label assignment from the main list</done>
</task>

<task type="auto">
  <name>Task 3: Implement Navigation Drawer for Filtering</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Add a `Drawer` to `ItemListScreen`.
    - Section for "All Items" (default).
    - Section for "Folders" and "Tags" (Labels).
    - Clicking a label updates the filter state in `ItemListScreen`.
    - When a label is selected, update the `StreamBuilder` to use a new database query (need to implement `watchItemsByLabel`).
  </action>
  <verify>
    <manual>Select a label in the drawer and verify only items with that label are shown</manual>
  </verify>
  <done>List can be filtered by label/folder via the sidebar</done>
</task>

<task type="auto">
  <name>Task 4: Implement watchItemsByLabel in Database</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
    Add `watchItemsByLabel(int labelId)` to `AppDatabase`.
    - Join `mnemata_items` with `item_labels` to filter by `labelId`.
  </action>
  <verify>
    <automated>Add unit test to verify filtered stream returns correct items</automated>
  </verify>
  <done>Database supports filtered item streams</done>
</task>

</tasks>

<verification>
Check that selecting a label in the drawer correctly filters the list and that assignments are persisted.
</verification>

<success_criteria>
- Navigation drawer shows all created labels.
- Selecting a label filters the main list items.
- Items can be assigned/unassigned from multiple labels seamlessly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-organization-continuity/03-02-SUMMARY.md`
</output>
