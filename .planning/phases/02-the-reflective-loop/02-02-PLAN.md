---
phase: 02-the-reflective-loop
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [lib/features/ingestion/services/extraction_service.dart, lib/features/ingestion/services/share_service.dart, test/features/ingestion/extraction_service_test.dart]
autonomous: true
requirements: [CON-01, CON-02]
must_haves:
  truths:
    - "Saved URLs are processed to extract main content"
    - "Clean content and updated titles are saved to the database"
  artifacts:
    - path: "lib/features/ingestion/services/extraction_service.dart"
      provides: "URL fetching and readability parsing"
    - path: "test/features/ingestion/extraction_service_test.dart"
      provides: "Unit tests for extraction logic"
  key_links:
    - from: "ExtractionService"
      to: "AppDatabase"
      via: "update method"
---

<objective>
Implement automatic content extraction for saved URLs.

Purpose: Transforming raw URLs into clean, ad-free text to enable offline reading and provide searchable content.
Output: ExtractionService and integration with the ingestion flow.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_2_THE_LOOP.md
@.planning/phases/02-the-reflective-loop/02-01-SUMMARY.md
@lib/core/database/app_database.dart
@lib/features/ingestion/services/share_service.dart

<interfaces>
From lib/core/database/app_database.dart:
```dart
Stream<List<MnemataItem>> watchAllItems();
Future<int> insertItem(MnemataItemsCompanion item);
// After 02-01:
Future<void> updateItemContent(int id, String content, String? title);
```

From lib/features/ingestion/services/share_service.dart:
```dart
class ShareService {
  Future<void> _handleSharedIntent(dynamic intent);
  Future<void> _processUrl(String url);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ExtractionService</name>
  <files>lib/features/ingestion/services/extraction_service.dart, test/features/ingestion/extraction_service_test.dart</files>
  <behavior>
    - ExtractionService must fetch HTML content from a URL.
    - It must use `dart_readability` to extract the main content and title.
    - It must handle network errors and parsing failures gracefully.
  </behavior>
  <action>
    Create `lib/features/ingestion/services/extraction_service.dart` with an `extractContent(String url)` method.
    The method should return a record or object containing `title` and `content`.
    Write unit tests in `test/features/ingestion/extraction_service_test.dart` using mock HTML content.
  </action>
  <verify>
    <automated>flutter test test/features/ingestion/extraction_service_test.dart</automated>
  </verify>
  <done>ExtractionService is implemented and verified with tests</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Extraction with Ingestion Flow</name>
  <files>lib/features/ingestion/services/share_service.dart</files>
  <action>
    Update `ShareService` to trigger `ExtractionService.extractContent` after a URL is successfully inserted into the database.
    Once extracted, the database record should be updated with the `content` and potentially the improved `title` from the reader view.
    Consider implementing this as an asynchronous post-ingestion task to keep the ingestion UI responsive.
  </action>
  <verify>
    <automated>flutter test test/core/database/app_database_test.dart</automated>
  </verify>
  <done>New URLs are automatically processed for content extraction</done>
</task>

</tasks>

<verification>
Check that saving a URL results in its content being fetched and stored in the database shortly after.
</verification>

<success_criteria>
- Saved URLs have their main content extracted and stored.
- Extraction process doesn't block the UI or fail silently.
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-reflective-loop/02-02-SUMMARY.md`
</output>
