---
phase: 02-the-reflective-loop
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [lib/features/reader/presentation/reader_screen.dart, lib/features/chronological_list/presentation/item_list_screen.dart]
autonomous: false
requirements: [CON-02, CON-03, ORG-04]
must_haves:
  truths:
    - "User can search for items by keywords in the main list"
    - "User can tap on a URL item to open the clean Reader view"
    - "Extracted content is rendered correctly without ads or clutter"
  artifacts:
    - path: "lib/features/reader/presentation/reader_screen.dart"
      provides: "The clean reader UI"
    - path: "lib/features/chronological_list/presentation/item_list_screen.dart"
      provides: "Search integrated list UI"
  key_links:
    - from: "ItemListScreen"
      to: "ReaderScreen"
      via: "Navigator.push"
---

<objective>
Implement the search UI and the reader experience for extracted content.

Purpose: Provide a clean, focused interface for reading saved articles and enable discovery through a searchable collection.
Output: Searchable list UI and a dedicated Reader screen.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
@/home/xian/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_2_THE_LOOP.md
@.planning/phases/02-the-reflective-loop/02-01-SUMMARY.md
@.planning/phases/02-the-reflective-loop/02-02-SUMMARY.md
@lib/features/chronological_list/presentation/item_list_screen.dart

<interfaces>
From lib/core/database/app_database.dart:
```dart
Stream<List<MnemataItem>> watchAllItems();
// After 02-01:
Stream<List<MnemataItem>> searchItems(String query);
```

From lib/features/chronological_list/presentation/item_list_screen.dart:
```dart
class ItemListScreen extends StatelessWidget {
  // Existing state management (StreamBuilder)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ReaderScreen</name>
  <files>lib/features/reader/presentation/reader_screen.dart</files>
  <action>
    Create `lib/features/reader/presentation/reader_screen.dart` to display extracted content.
    - Use `flutter_widget_from_html` to render the `item.content`.
    - Handle cases where `item.content` is null (e.g., show a placeholder or the URL).
    - Design should be clean and optimized for reading (Material 3).
  </action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <done>Reader screen is implemented and displays clean content</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Search UI</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Update `ItemListScreen` to include a search bar in the `AppBar`.
    - Maintain search state (query string).
    - When the query is not empty, use `AppDatabase.searchItems(query)` as the stream for the `StreamBuilder`.
    - Otherwise, use the default `watchAllItems()` stream.
    - Ensure a clean "no results" state.
  </action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <done>List is searchable in real-time</done>
</task>

<task type="auto">
  <name>Task 2: Update Navigation Logic</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    Modify `_handleOpen` in `ItemListScreen` to navigate to `ReaderScreen` if the item has `content`.
    - If `item.type == 'url'` and `item.content != null`, navigate to `ReaderScreen`.
    - Otherwise, keep existing behavior (open in external browser or file viewer).
  </action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <done>Tapping items with extracted content opens the Reader view</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Search and Reader experience</what-built>
  <how-to-verify>
    1. Share a URL to the app.
    2. Wait for extraction to complete.
    3. Tap the item in the list and verify it opens in the Reader view (no browser window).
    4. Type a keyword from the article in the search bar and verify the item appears.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check that search works as expected and that the reader view is functional and well-formatted.
</verification>

<success_criteria>
- Search returns relevant items based on content.
- URL items with content open the internal Reader view.
- Reader view provides a clean, focused experience.
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-reflective-loop/02-03-SUMMARY.md`
</output>
