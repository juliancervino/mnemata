---
phase: 01-foundation-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/database/app_database.dart
  - lib/core/database/tables.dart
autonomous: true
requirements: [ING-01, ING-02]
must_haves:
  truths:
    - "Flutter project is initialized and dependencies are installed"
    - "Drift database is defined with an Items table"
    - "Database code generation completes successfully"
  artifacts:
    - path: "lib/core/database/app_database.dart"
      provides: "Drift database singleton"
    - path: "lib/core/database/tables.dart"
      provides: "Schema for Mnemata items"
  key_links:
    - from: "lib/core/database/app_database.dart"
      to: "drift_flutter"
      via: "driftDatabase initialization"
---

<objective>
Initialize the Flutter project and establish the local persistence layer using Drift (SQLite).

Purpose: Provides the foundational storage for all ingested content.
Output: Working Flutter project with a reactive database schema.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_1_FLUTTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization & Dependencies</name>
  <files>pubspec.yaml</files>
  <action>
    Initialize the Flutter project in the current directory (if not already done).
    Add the following dependencies to pubspec.yaml:
    - drift: ^2.16.0
    - drift_flutter: ^0.2.0
    - path_provider: ^2.1.2
    - path: ^1.9.0
    - receive_sharing_intent_plus: ^0.1.0
    - url_launcher: ^6.2.5
    - open_file_plus: ^3.3.1

    Add dev_dependencies:
    - drift_dev: ^2.16.0
    - build_runner: ^2.4.9

    Run `flutter pub get`. Create the following directory structure:
    - lib/core/database
    - lib/features/ingestion/services
    - lib/features/chronological_list/presentation
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>Flutter project is ready with all necessary packages installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database Implementation</name>
  <files>lib/core/database/tables.dart, lib/core/database/app_database.dart</files>
  <behavior>
    - MnemataItems table should have: id (auto-increment), title (text, nullable), url (text, nullable), filePath (text, nullable), type (text: 'url' or 'file'), createdAt (datetime).
    - Database should expose a stream to watch all items ordered by createdAt descending.
    - Database should have a method to insert an item.
  </behavior>
  <action>
    1. Create `lib/core/database/tables.dart` with the MnemataItems table definition.
    2. Create `lib/core/database/app_database.dart` following the 2024 pattern from research (using `driftDatabase(name: 'mnemata_db')`).
    3. Run `flutter pub run build_runner build --delete-conflicting-outputs` to generate the database code.
    4. Implement `watchAllItems()` and `insertItem(MnemataItemsCompanion item)`.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Drift database is implemented and code is generated.</done>
</task>

</tasks>

<success_criteria>
- `flutter pub get` succeeds.
- Database code generation succeeds without errors.
- Schema matches requirements for Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ingestion/01-01-SUMMARY.md`
</output>
