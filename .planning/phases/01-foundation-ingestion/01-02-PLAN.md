---
phase: 01-foundation-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - ios/Runner/Info.plist
  - lib/features/ingestion/services/share_service.dart
  - lib/main.dart
autonomous: false
requirements: [ING-03]
user_setup:
  - service: "iOS App Groups"
    why: "Required for iOS Share Extensions to access shared files"
    dashboard_config:
      - task: "Create App Group in Apple Developer Portal"
        location: "Certificates, Identifiers & Profiles -> Identifiers -> App Groups"
must_haves:
  truths:
    - "App appears in system share menus on Android and iOS"
    - "Sharing text or files to Mnemata triggers the ingestion logic"
    - "Shared files are copied to the app's local document directory"
  artifacts:
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Android intent filter configuration"
    - path: "lib/features/ingestion/services/share_service.dart"
      provides: "Platform-agnostic ingestion handler"
  key_links:
    - from: "ReceiveSharingIntentPlus"
      to: "ShareService"
      via: "Media streams"
---

<objective>
Configure native Android and iOS platforms to receive share intents and implement a service to handle incoming content.

Purpose: Enables the primary ingestion mechanism for the app.
Output: App appears in system share menus and processes incoming URLs/Files.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_1_FLUTTER.md
@.planning/phases/01-foundation-ingestion/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android & iOS Platform Configuration</name>
  <files>android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <action>
    - Update `android/app/src/main/AndroidManifest.xml` with `android:launchMode="singleTask"`.
    - Add intent-filters to `AndroidManifest.xml` for `SEND` and `SEND_MULTIPLE` for both text/plain and application/pdf.
    - Update `ios/Runner/Info.plist` with `CFBundleDocumentTypes` and `UISupportsDocumentBrowser`.
    - Note: User must manually configure App Groups in Xcode if they want to test on real iOS devices.
  </action>
  <verify>
    <automated>grep "singleTask" android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>Native manifests are updated for share intent support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ingestion Service Implementation</name>
  <files>lib/features/ingestion/services/share_service.dart, lib/main.dart</files>
  <behavior>
    - Service should listen to `ReceiveSharingIntentPlus.getMediaStream()` and `ReceiveSharingIntentPlus.getInitialMedia()`.
    - Incoming text should be saved as a URL item in Drift.
    - Incoming files should be copied to `getApplicationDocumentsDirectory()` and saved as a File item in Drift.
    - Duplicate checks (same URL or path) should be performed before insertion.
  </behavior>
  <action>
    1. Create `ShareService` in `lib/features/ingestion/services/share_service.dart`.
    2. Inject the Drift `AppDatabase` into `ShareService`.
    3. Implement `init()` method that sets up the listeners.
    4. Update `lib/main.dart` to initialize the database and the share service on startup.
  </action>
  <verify>
    <automated>flutter test test/share_service_test.dart (if testable, otherwise manual check below)</automated>
  </verify>
  <done>ShareService handles incoming content and persists it to the database.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Android and iOS native share intent configuration and ingestion service.</what-built>
  <how-to-verify>
    1. Run the app on an Android emulator or device.
    2. Open a browser and share a URL to 'Mnemata'.
    3. Verify the app opens and (internally) the URL is saved (check logs or debug console).
    4. Repeat with a file sharing.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- App appears in the "Share" menu on Android/iOS.
- Sharing a URL inserts a row into the Drift database.
- Sharing a PDF copies the file to the app's document directory and inserts a row.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ingestion/01-02-SUMMARY.md`
</output>
