---
phase: 01-foundation-ingestion
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/features/chronological_list/presentation/item_list_screen.dart
  - lib/main.dart
autonomous: false
requirements: [ORG-04]
must_haves:
  truths:
    - "Main UI displays a scrollable list of ingested items"
    - "Items are sorted by creation date (newest first)"
    - "Tapping an item opens the corresponding URL or file"
  artifacts:
    - path: "lib/features/chronological_list/presentation/item_list_screen.dart"
      provides: "Main application screen"
  key_links:
    - from: "AppDatabase"
      to: "ItemListScreen"
      via: "StreamBuilder"
    - from: "ItemListScreen"
      to: "url_launcher"
      via: "launchUrl"
---

<objective>
Implement the main chronological list UI and item interaction (tap-to-open).

Purpose: Provides the primary user interface for viewing and accessing saved content.
Output: Scrollable list of items sorted by time, with working links/files.
</objective>

<execution_context>
@/home/xian/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_1_FLUTTER.md
@.planning/phases/01-foundation-ingestion/01-01-PLAN.md
@.planning/phases/01-foundation-ingestion/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chronological Item List UI</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    - Create a `ItemListScreen` widget.
    - Use `StreamBuilder` connected to the `watchAllItems()` stream from the Drift database.
    - Render a `ListView.builder` displaying items in cards or tiles.
    - Each tile should show the item's title (or URL/filename if title is null), type icon (link or file), and a formatted date.
    - Sort should already be handled by the database query.
  </action>
  <verify>
    <automated>flutter test test/item_list_screen_test.dart (Widget test for list presence)</automated>
  </verify>
  <done>Main UI displays a list of items from the database in chronological order.</done>
</task>

<task type="auto">
  <name>Task 2: Item Interaction (Tap to Open)</name>
  <files>lib/features/chronological_list/presentation/item_list_screen.dart</files>
  <action>
    - Add an `onTap` handler to each item tile.
    - If the item is a URL: Use `url_launcher` to open it in the default system browser.
    - If the item is a File: Use `open_file_plus` to open it with the system's associated application.
    - Handle potential errors (e.g., file not found or no app to handle URL) by showing a Snackbar.
  </action>
  <verify>
    <automated>grep "url_launcher" lib/features/chronological_list/presentation/item_list_screen.dart</automated>
  </verify>
  <done>Tapping an item correctly opens the URL or the file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 1 integration: Share Intent -> Database -> UI -> Open.</what-built>
  <how-to-verify>
    1. Open the app.
    2. Share a new URL from a browser.
    3. Verify the URL appears at the top of the list in the app.
    4. Tap the URL in the list and verify it opens in the browser.
    5. Share a PDF file from a file explorer.
    6. Verify the PDF appears in the list.
    7. Tap the PDF in the list and verify it opens in a PDF viewer.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- User can see a chronological list of saved items.
- Newest items are at the top.
- Tapping an item opens the corresponding content.
- All Phase 1 requirements (ING-01, ING-02, ING-03, ORG-04 part) are met.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ingestion/01-03-SUMMARY.md`
</output>
